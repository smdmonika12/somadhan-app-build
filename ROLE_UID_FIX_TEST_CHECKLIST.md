# Role-UID Sync Fix — ধাপ ৬: Manual Regression Test Checklist

এই চেকলিস্টটা `ROLE_UID_SYNC_FIX_MASTER_PROMPT.md`-এর ধাপ ৬ অনুযায়ী তৈরি — কোনো নতুন কোড লেখা
হয়নি, শুধু ধাপ ০–৫-এ যা যা ঠিক/verify করা হয়েছে তার বাস্তব ডিভাইসে ভেরিফিকেশনের জন্য একটা
checklist। প্রতিটা কেসের পাশে ✅/❌ মার্ক করে রাখো, fail হলে কোন ধাপে/কী দেখে বুঝলে সেটা এক লাইনে
নোট করে রাখলে পরের ফিক্স-সেশনে কাজে লাগবে।

**Prerequisite:** দুইটা আলাদা ডিভাইস/ইমুলেটর (A ও B) লাগবে, প্লাস admin panel অ্যাক্সেস। টেস্টের
আগে দুই ডিভাইসেই ফ্রেশ বিল্ড ইনস্টল থাকা ভালো।

---

## ১. ডিভাইস A — নতুন অ্যাকাউন্ট থেকে বেসিক ফ্লো (single-device sanity)

- [ ] নতুন অ্যাকাউন্ট সাইনআপ/লগইন (OTP দিয়ে) — লগইনের পর app ক্র্যাশ/স্টাক হয় না।
- [ ] KYC সাবমিট করা — সাবমিট করার পর status ঠিকমতো "pending" দেখাচ্ছে স্থানীয়ভাবে।
- [ ] User role-এ একটা problem/job পোস্ট করা — লিস্টে ঠিকমতো দেখাচ্ছে।
- [ ] Role switch করে Solver-এ যাওয়া (`switchRoleToSolver`) — switch-এর পর app স্টাক/ক্র্যাশ হয়
      না, প্রোফাইল/হোম স্ক্রিন ঠিকমতো solver view দেখায়।
- [ ] Solver role-এ ওই problem-এ বিড করা — বিড লিস্টে/নিজের বিড history-তে দেখাচ্ছে।
- [ ] আবার Role switch করে User-এ ফেরত যাওয়া (`switchRoleToUser`) — নিজের পোস্ট করা problem আর
      তার বিডগুলো এখনও ঠিকমতো দেখাচ্ছে (হারিয়ে যায়নি)।
- [ ] **সবচেয়ে গুরুত্বপূর্ণ চেক:** role switch করার আগে আর পরে ইউজারের `id`/`displayUid` একই
      আছে কিনা — সম্ভব হলে প্রোফাইল স্ক্রিনে UID visible থাকলে সেটা নোট করে দুইবার (switch-এর
      আগে ও পরে) মিলিয়ে দেখো। আলাদা হয়ে গেলে এটা মূল বাগ এখনও আছে বলে ধরে নিতে হবে।

## ২. ডিভাইস B — একই অ্যাকাউন্ট, cross-device দেখা যাচ্ছে কিনা

- [ ] ডিভাইস B-তে একই অ্যাকাউন্টে (একই ফোন নম্বর/OTP দিয়ে) লগইন করা।
- [ ] ডিভাইস A-তে সাবমিট করা KYC status ডিভাইস B-তে দেখা যাচ্ছে (pending/approved যা-ই থাকুক)।
- [ ] ডিভাইস A-তে পোস্ট করা problem/job ডিভাইস B-তে দেখা যাচ্ছে।
- [ ] ডিভাইস A-তে দেওয়া বিড ডিভাইস B-তে (solver history বা problem-এর বিড লিস্টে) দেখা যাচ্ছে।
- [ ] ডিভাইস A-তে থাকা balance (user/solver — role অনুযায়ী যেটা প্রযোজ্য) ডিভাইস B-তে একই
      দেখাচ্ছে।
- [ ] ডিভাইস B-তে role switch করলে সেটাও ডিভাইস A-এর মতোই স্মুথভাবে কাজ করে (স্টাক/ক্র্যাশ হয়
      না), এবং ডিভাইস B-তেও role switch-এর আগে-পরে id/UID অপরিবর্তিত থাকে।

## ৩. ডিভাইস A-তে পরিবর্তন → ডিভাইস B-তে refresh করে দেখা (real-time/pull sync)

- [ ] ডিভাইস A-তে role switch করা (User→Solver বা উল্টো) — এরপর ডিভাইস B-তে app refresh/pull-to-
      refresh করে দেখা যে ডিভাইস B-ও এখন ডিভাইস A-এর current role ঠিকমতো দেখাচ্ছে (বা অন্তত
      role-নির্ভর ডেটা সঠিকভাবে sync হয়েছে)।
- [ ] ডিভাইস A-তে নতুন একটা message পাঠানো (কোনো active problem/chat-এ) — ডিভাইস B-তে সেই
      message দেখা যাচ্ছে।
- [ ] ডিভাইস A-তে notification mark-as-read করা — ডিভাইস B-তে (refresh-এর পর) সেই notification-ও
      read দেখাচ্ছে।
- [ ] ডিভাইস A-তে instant job flow-এর কোনো ধাপ (post/accept/on-way/arrived/job-started) করলে
      ডিভাইস B-তে সেই status আপডেট refresh-এর পর দেখা যাচ্ছে।
- [ ] ডিভাইস A-তে withdrawal/deposit/extra-charge request করলে ডিভাইস B-তে ওয়ালেট/ব্যালেন্স
      history-তে সেটা দেখা যাচ্ছে।

## ৪. Admin panel ↔ ডিভাইস sync

- [ ] Admin panel থেকে ডিভাইস A/B-এর ইউজারের KYC approve করা — ডিভাইস-এ (refresh/relogin-এর পর)
      status "approved" দেখাচ্ছে।
- [ ] Admin panel থেকে একই ইউজারের KYC reject করা — ডিভাইসে reject reason/status ঠিকমতো
      দেখাচ্ছে।
- [ ] Admin panel-এ ওই ইউজারের প্রোফাইল/balance/role দেখলে সেটা ডিভাইসে যা আছে তার সাথে মেলে
      (role switch করার পরেও)।
- [ ] Admin panel থেকে withdrawal/dispute-জাতীয় কোনো অ্যাকশন নিলে (যদি টেস্ট অ্যাকাউন্টে
      প্রযোজ্য হয়) সেটা ইউজারের ডিভাইসে notification/status আকারে পৌঁছাচ্ছে।

## ৫. পুরনো (migration-এর আগের) টেস্ট অ্যাকাউন্ট, যদি থাকে

- [ ] এমন কোনো পুরনো টেস্ট অ্যাকাউন্ট থাকলে যেটাতে আগে থেকেই `SOLVER_xxx`/`USER_xxx` prefix-এর
      আলাদা linked row ছিল (role switch করে তৈরি হওয়া পুরনো ডেটা) — সেই অ্যাকাউন্টে এই নতুন
      বিল্ড দিয়ে লগইন করা।
- [ ] লগইনের পর `migrateLegacyDualRowsIntoRoot()` চলার কথা — root row-এ KYC status,
      solverCategories, balance ঠিকমতো merge হয়েছে কিনা যাচাই করা (কোনো ফিল্ড ফাঁকা/হারিয়ে
      যায়নি)।
- [ ] Migration-এর পর ওই অ্যাকাউন্টে role switch করে দেখা — আগের মতোই স্বাভাবিকভাবে কাজ করছে
      কিনা, এবং আগে থেকে থাকা balance/KYC/categories ডেটা অক্ষত আছে কিনা।
- [ ] (যদি সম্ভব হয়) migration-এর আগে ও পরে root row আর পুরনো linked row দুটোই দেখে নিশ্চিত হওয়া
      যে পুরনো row ডিলিট হয়ে যায়নি, শুধু "migrated" হিসেবে চিহ্নিত হয়েছে (rollback সম্ভব থাকার
      কথা)।
- [ ] এমন কোনো পুরনো টেস্ট অ্যাকাউন্ট না থাকলে এই সেকশনটা "N/A — কোনো পুরনো অ্যাকাউন্ট ছিল না"
      লিখে স্কিপ করে দিতে পারো।

---

## ফলাফল রিপোর্ট করার সময়

প্রতিটা সেকশনে যা ফেল করেছে সেটার জন্য জানিও:
- কোন কেসটা (উপরের বুলেট অনুযায়ী)
- কী আশা করেছিলে vs. আসলে কী হলো
- কোন role/ডিভাইসে হয়েছে (A/B, User/Solver)

কিছু fail করলে সেটা নিয়ে একটা আলাদা ছোট ফিক্স-ধাপ শুরু হবে (ধাপ ৭-এর আগেই) — পুরো ৫টা সেকশন
একসাথে re-run করার দরকার নেই, শুধু fail হওয়া কেসটা relevant সেকশন ধরে আবার চেক করলেই হবে।
