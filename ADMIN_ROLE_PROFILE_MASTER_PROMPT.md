# সমাধান অ্যাপ — এডমিন রোল ম্যানেজমেন্ট, ফ্ল্যাগ, অ্যাক্টিভিটি লগ ও প্রোফাইল সিস্টেম — মাস্টার প্রম্পট

> **এই ফাইল কেন:** Claude চ্যাটে দুটো স্ট্যান্ডঅ্যালোন HTML মকআপ বানানো হয়েছে —
> `admin-role-management.html` (রোল CRUD + পারমিশন ট্রি + এডমিন অ্যাকাউন্ট + ফ্ল্যাগ + অ্যাক্টিভিটি লগ +
> লাইভ প্রিভিউ) আর `admin-profile.html` (প্রতিটা এডমিনের/সুপার অ্যাডমিনের প্রোফাইল — ছবি আপলোড, পদবি,
> পারমিশন চিপ, লগইন তথ্য, সেশন, সিকিউরিটি)। এই দুটো ফাইল **শুধু UI/UX ও ফিচার-স্পেক রেফারেন্স** —
> ভেতরের JS/CSS/ফেক ডেটা সরাসরি কপি করার জন্য না। আসল কোডবেস (আপলোড করা zip,
> `somadhan-step8-pending-windows-run.zip`) গ্রেপ করে যা পাওয়া গেছে তার ভিত্তিতে এই ফাইলটা লেখা হলো,
> যাতে Claude Code (বা যেকোনো ভবিষ্যৎ সেশন) সরাসরি বাস্তবায়ন পরিকল্পনা নিয়ে কাজ শুরু করতে পারে।
>
> **সহ-শর্ত:** নতুন/retrofit হওয়া যেকোনো স্ক্রিনে loading/shimmer আচরণ অবশ্যই
> `SOMADHAN_LOADING_PATTERN_MASTER_PROMPT.md` + `SHIMMER_REGRESSION_BATCH31_MASTER_PROMPT.md` +
> `ADMIN_PANEL_LOADING_MASTER_PROMPT.md`-এর Ground Rule মেনে হতে হবে (এই ফাইল সেগুলো পুনরায় লেখেনি,
> শুধু রেফারেন্স করছে) — `SyncAwareContent`/`rememberSessionAwareSkeletonGate`, per-item pulse
> (`flashOnReentry = false`), re-entry-তে full-tab flash নিষেধ, ইত্যাদি অপরিবর্তিত থাকবে।

---

## ধাপ ০ — বর্তমান কোডবেসে যা পাওয়া গেছে (zip গ্রেপ-ভেরিফায়েড, অনুমান না)

1. **এই মুহূর্তে মাল্টি-এডমিন/রোল সিস্টেম বলে কিছু নেই।** `AdminCredentials.kt`-এ পুরো অ্যাপের জন্য
   **একটাই** admin phone+password (DataStore + `admin_credentials` টেবিল, RPC:
   `admin_credentials_get_phone`, `admin_credentials_verify_password`, `admin_credentials_update`)।
   কোনো "কে লগইন করেছে" concept নেই, তাই কোনো স্ক্রিনেই এখন পারমিশন-চেক নেই — একজন যিনিই লগইন করুন,
   সব ২৫টা ট্যাবে পূর্ণ অ্যাক্সেস।
2. **অ্যাক্টিভিটি লগ ইতিমধ্যে আংশিক আছে** — `admin_audit_logs` টেবিল + `log_admin_action(...)` RPC
   (দুটো overload: with/without `p_role`), ব্যবহৃত হচ্ছে `AdminUsersView`, `AdminKycView` ইত্যাদি অনেক
   স্ক্রিন থেকে, দেখানো হয় `AdminAuditLogView.kt`-তে (ট্যাব ইনডেক্স ১২)। কিন্তু **কোনো admin identity
   কলাম নেই** (`AdminAuditLogEntity`/`AdminAuditLogDto` দেখুন) — শুধু `action_type, target_id,
   target_name, details, timestamp, role`। `role` কলামটা user/solver role (ধাপ: `"USER"/"SOLVER"/""`),
   admin-role না — নাম সাংঘর্ষিক, নতুন স্কিমায় সতর্ক থাকতে হবে।
3. **ব্র্যান্ড কালার মিলে গেছে** — `Color.kt`-তে `SomadhanOrange = Color(0xFFFF6500)`, মকআপেও একই হেক্স
   ব্যবহার হয়েছে, তাই ভিজ্যুয়াল দিক থেকে দুটো মকআপ আসল অ্যাপের সাথে সামঞ্জস্যপূর্ণ।
4. **ছবি আপলোডের রেডিমেড প্যাটার্ন আছে** — `ImageStorageUtil.kt`, `KycUploadManager.kt`। প্রোফাইল ফটোর
   জন্য নতুন কিছু বানানোর দরকার নেই, একই প্যাটার্নে নতুন bucket (যেমন `admin-profile-photos`) ব্যবহার
   করলেই হবে।
5. **`AdminPanelScreen.kt`-এর ড্রয়ার গ্রুপ/ইনডেক্স ম্যাপ** (২০২৬-০৯-২৪ পর্যন্ত zip অনুযায়ী, ইনডেক্স
   ০-২৪ ব্যবহৃত — পরবর্তী ফ্রি ইনডেক্স ২৫ থেকে): ড্যাশবোর্ড, ইউজার ব্যবস্থাপনা, কাজ ও চুক্তি, আর্থিক
   ব্যবস্থাপনা, কনফিগারেশন, বিরোধ ব্যবস্থাপনা, যোগাযোগ, মডারেশন, সিস্টেম/অ্যাডভান্সড — এই গ্রুপিং-ই
   মকআপ ১-এর `GROUPS` অবজেক্টের কাঠামো বানানোর সময় রেফারেন্স হিসেবে ব্যবহার হয়েছিল, তাই বাস্তবায়নের
   সময় নাম/গ্রুপিং প্রায় হুবহু মিলবে — এটা কাকতালীয় না, ইচ্ছাকৃত মিল।
6. **কোথাও কোনো পারমিশন-চেক নেই** — প্রতিটা `AdminXxxView.kt`-এর সব বাটন unconditionally রেন্ডার হয়।
   রিয়েল পারমিশন সিস্টেম বসাতে হলে প্রতিটা স্ক্রিনে রিট্রফিট লাগবে (ধাপ ৩-এ বিস্তারিত)।

---

## ধাপ ১ — দুটো মকআপ থেকে ফিচার লিস্ট (source of truth)

### `admin-role-management.html`
- রোল CRUD: নাম + প্রতিটা মেনু-গ্রুপ/মেনু-আইটেম/অ্যাকশনের (view/create/edit/delete/approve/reject/
  ban/restrict/...) গ্র্যানুলার পারমিশন ট্রি। কিছু গ্রুপ (`superOnly`) শুধু সুপার অ্যাডমিনের জন্য।
- এডমিন অ্যাকাউন্ট CRUD: নাম, ফোন, রোল অ্যাসাইন, active/inactive টগল — **সুপার অ্যাডমিন অ্যাকাউন্ট
  কখনো নিষ্ক্রিয় করা যাবে না** (UI-তে লকড দেখায়)।
- **নতুন: ফ্ল্যাগ/আনফ্ল্যাগ টগল** — নন-সুপার অ্যাকাউন্টে। ফ্ল্যাগড অ্যাকাউন্ট তার রোল-অনুযায়ী সব মেনু
  দেখতে পারবে (view পারমিশন অক্ষত), কিন্তু কোনো অ্যাকশন বাটন (create/edit/delete/approve/ban/...) কাজ
  করবে না — সুপার অ্যাডমিন কখনো ফ্ল্যাগ করা যাবে না।
- অ্যাক্টিভিটি লগ ট্যাব, **শুধু সুপার অ্যাডমিন দেখবে** — প্রতিটা এন্ট্রির হেডলাইনে অ্যাডমিনের নাম +
  রোল ব্যাজ পাশাপাশি, তারপর অ্যাকশন ভার্ব + টার্গেট + কারণ + টাইমস্ট্যাম্প। ফিল্টার **অ্যাডমিনের নাম
  দিয়ে** (সার্চ ইনপুট + ড্রপডাউন সিলেক্ট, দুটোই) — লাইভ-ফিল্টার না, explicit "ফিল্টার করুন" বাটনে
  ক্লিক করলেই প্রয়োগ হয়।
- ডানপাশে লাইভ প্রিভিউ সিমুলেটর — কোনো রোল/নির্দিষ্ট অ্যাকাউন্ট (ফ্ল্যাগড অবস্থাসহ) লগইন করলে কী মেনু ও
  কোন অ্যাকশন বাটন দেখবে তার প্রিভিউ। **প্রোডাকশনে শিপ করা হবে কিনা confirm করা দরকার** (ধাপ ৫,
  প্রশ্ন ২) — মকআপে এটা রোল-বিল্ডিং সহজ করার ডিজাইন-টুল হিসেবে বানানো হয়েছিল।

### `admin-profile.html`
- প্রতিটা এডমিনের (সুপার অ্যাডমিনসহ) নিজস্ব প্রোফাইল — নাম ও পদবি এডিটেবল, প্রোফাইল ছবি আপলোড
  (ক্যামেরা-আইকন → ফাইল পিকার → লাইভ প্রিভিউ)। ফোন নম্বর লকড/read-only। ইমেইল এডিটেবল। সংক্ষিপ্ত
  পরিচিতি (bio)।
- **পদবি অনুযায়ী ফাংশন এক্সেস** — read-only চিপ লিস্ট, রোল থেকে ডেরাইভড, এখান থেকে এডিট করা যায় না।
- লগইন তথ্য: সর্বশেষ লগইন টাইমস্ট্যাম্প, ডিভাইস, আইপি, অনলাইন/অফলাইন স্ট্যাটাস ডট।
- সাম্প্রতিক সেশন লিস্ট (ডিভাইস + লোকেশন + সময়, একাধিক এন্ট্রি)।
- সিকিউরিটি সেকশন: 2FA টগল (ভিজ্যুয়াল প্লেসহোল্ডার), পাসওয়ার্ড পরিবর্তন বাটন।
- ফ্ল্যাগড হলে লাল ব্যানার (উপরের ফ্ল্যাগ ফিচারের সাথে সরাসরি যুক্ত)।
- বামে সব এডমিনের সার্চেবল লিস্ট, ক্লিক করে যে কারো প্রোফাইল দেখা যায় — **এটা প্রোডাকশনে সবাই সবার
  দেখতে পারবে, নাকি শুধু নিজের প্রোফাইল (সুপার অ্যাডমিন ছাড়া)?** confirm করা দরকার (ধাপ ৫, প্রশ্ন ৬)।

---

## ধাপ ২ — প্রস্তাবিত Supabase স্কিমা (কোনো migration এখনো লেখা হয়নি, কনফার্মেশন ছাড়া শুরু না)

```sql
-- রোল সংজ্ঞা
admin_roles (
  id            text primary key,   -- 'super' + কাস্টম রোলের slug
  name          text not null,
  is_super      boolean not null default false,
  permissions   jsonb not null default '[]',  -- ["users:users:view", "users:users:ban", ...]
  created_at    timestamptz default now(),
  updated_at    timestamptz default now()
)

-- এডমিন অ্যাকাউন্ট (নতুন — এটাই মাল্টি-এডমিন-এর ভিত্তি)
admin_accounts (
  id                uuid primary key default gen_random_uuid(),
  name              text not null,
  designation       text default '',
  phone             text unique not null,
  password_hash     text not null,     -- admin_credentials-এর bcrypt প্যাটার্ন অনুসরণ
  email             text,
  photo_url         text,
  bio               text default '',
  role_id           text references admin_roles(id),
  active            boolean not null default true,
  flagged           boolean not null default false,
  flagged_at        timestamptz,
  flagged_by        uuid references admin_accounts(id),
  last_login_at     timestamptz,
  last_login_device text,
  last_login_ip     text,
  created_at        timestamptz default now()
)
-- DB-লেভেল গার্ড (শুধু UI না): সুপার অ্যাডমিন রো-তে active=false বা flagged=true সেট করতে গেলে
-- trigger/CHECK দিয়ে ব্লক করতে হবে — client bug/RPC misuse হলেও সুপার অ্যাডমিন অক্ষুণ্ণ থাকে।

-- (ঐচ্ছিক, "সাম্প্রতিক সেশন" লিস্টের জন্য — শুধু last_login_* কলাম যথেষ্ট কিনা প্রথমে ভাবা দরকার)
admin_sessions (
  id            uuid primary key default gen_random_uuid(),
  admin_id      uuid references admin_accounts(id),
  device        text, location text,
  logged_in_at  timestamptz default now()
)
```

`admin_audit_logs`-এ **alter** লাগবে: `admin_id uuid references admin_accounts(id)`,
`admin_name text` (denormalized, লগ পুরনো হলেও নাম দেখাতে)। `details`-এ "কারণ" আলাদা কলামে যাবে নাকি
বিদ্যমান `details` টেক্সটেই ফরম্যাট থাকবে — ধাপ ৫, প্রশ্ন ৪-এ কনফার্ম করা দরকার।

### RPC (SECURITY DEFINER, `admin_credentials_*`-এর প্যাটার্নে)
- `admin_login(p_phone, p_password)` → সফল হলে account row (id, name, role_id, permissions) ফেরত,
  পাশাপাশি `last_login_at/device/ip` আপডেট + `admin_sessions`-এ insert।
- `admin_roles_list()`, `admin_role_upsert(...)`, `admin_role_delete(...)` — কলের ভেতরেই caller
  সুপার অ্যাডমিন কিনা যাচাই বাধ্যতামূলক।
- `admin_accounts_list()`, `admin_account_upsert(...)`, `admin_account_set_active(...)`,
  `admin_account_set_flagged(...)` — একই সুপার-অ্যাডমিন গার্ড + উপরের DB-trigger দুই স্তরে সুরক্ষা।
- `log_admin_action(...)`-এর নতুন overload/সংস্করণ লাগবে যেখানে `p_admin_id`/`p_admin_name` যোগ হবে —
  বিদ্যমান ১৫+ কল-সাইট (প্রতিটা `AdminXxxView.kt`/ViewModel-এ) ধাপে ধাপে আপডেট করতে হবে, এক সেশনে না
  (ধাপ ৪-এ আলাদা সেশন)।

---

## ধাপ ৩ — ক্লায়েন্ট (Kotlin/Compose) স্কোপ

- নতুন স্ক্রিন: `AdminRoleManagementView.kt` (ট্যাব, সুপার-অনলি, প্রস্তাবিত ইনডেক্স ২৫),
  `AdminProfileView.kt` (ট্যাব/ডেস্টিনেশন, ইনডেক্স ২৬, **সব রোলের জন্যই** visible — নিজের প্রোফাইল)।
- `AdminPanelScreen.kt`-এ নতুন ড্রয়ার গ্রুপ "অ্যাডমিন ব্যবস্থাপনা" (সুপার-অনলি কন্ডিশনালি রেন্ডার) —
  আইটেম: রোল ম্যানেজমেন্ট, এডমিন অ্যাকাউন্ট (একই স্ক্রিনের দুই ট্যাব হিসেবেও রাখা যায়, মকআপ ১-এর মতো)।
  "প্রোফাইল" আলাদাভাবে — যেহেতু সবাই দেখবে, group-এ পুঁতে না রেখে টপ বার/ড্রয়ার হেডারে সবসময়-দৃশ্যমান
  জায়গায় রাখাই ভালো (ধাপ ৫, প্রশ্ন ৬)।
- **সবচেয়ে বড় কাজ (সেশন ৭, ধাপ ৪-এ সাব-স্টেপসহ বিস্তারিত):** বিদ্যমান ২৫টা
  `AdminXxxView.kt`-এর প্রতিটায় রিয়েল পারমিশন-গেটিং বসানো — (ক) ট্যাব ওপেন হওয়ার আগে "view" চেক (না
  থাকলে মকআপের মতো "অ্যাক্সেস নেই" স্টেট), (খ) প্রতিটা অ্যাকশন বাটনে eligibility চেক, (গ) flagged
  অ্যাডমিনের জন্য **গ্লোবাল** অ্যাকশন-লক (রোল যাই বলুক)। সবচেয়ে পরিষ্কার উপায়: একটা শেয়ার্ড
  `LocalAdminSession`/helper (যেমন `AdminSession.canAct(actionKey)`) বানিয়ে প্রতিটা স্ক্রিনে ব্যবহার —
  ২৫টা ফাইলে ছড়িয়ে-ছিটিয়ে `if (isSuper || ...)` না লিখে।
- Loading pattern: নতুন স্ক্রিনগুলোও `SyncAwareContent`/per-item pulse কনভেনশন মেনে চলবে —
  `AdminUsersView`/`AdminAuditLogView`-কে রেফারেন্স ইমপ্লিমেন্টেশন হিসেবে ব্যবহার করা যায়।
- ছবি আপলোড: `ImageStorageUtil.kt`/`KycUploadManager.kt` প্যাটার্নে নতুন bucket।
- **ফাউন্ডেশনাল/ব্রেকিং সিদ্ধান্ত:** `AdminCredentials`-এর সিঙ্গেল গ্লোবাল লগইন গেট পুরো
  মাল্টি-এডমিন লগইন ফ্লো দিয়ে প্রতিস্থাপিত হবে (extend না, replace) — এটা সেশন ১-২, সতর্কভাবে, আর
  পুরনো ইনস্টলে migrate করার সময় বিদ্যমান `DEFAULT_ADMIN_PHONE`/হ্যাশ থেকে সুপার-অ্যাডমিন রো
  অটো-সিড করতে হবে যাতে কেউ লকড-আউট না হয়ে যায়।

---

## ধাপ ৪ — প্রস্তাবিত সেশন অর্ডার

> **⚠️ এই ব্লকের "সেশন ১-৬ টানা" নিয়ম PROGRESS ফাইলে ২০২৬-০৯-২৪-এ ওভাররাইড হয়ে গেছে (প্রতিটা সেশন
> আলাদাভাবে, প্রতিটার পর থেমে কনফার্মেশন) — আর ২০২৬-০৯-২৫-এ PROGRESS ফাইলে আরেকটা নিয়ম যোগ হয়েছে:
> সেশন ৭.০-৭.৯-এর **প্রতিটা সাব-স্টেপ শেষে updated zip (dotfile/hidden ফাইলসহ) দিতে হবে**, তারপর
> কনফার্মেশনের জন্য থামতে হবে। PROGRESS ফাইলের শুরুর ক্যাডেন্স-নোটই এখানে চূড়ান্ত সত্য।**
>
> **কনফার্মেশন-ক্যাডেন্স (ব্যবহারকারীর সিদ্ধান্ত, ২০২৬-০৯-২৪, নিচেরটা এখন ঐতিহাসিক রেফারেন্স):** সেশন ১-৬ **টানা চলবে, মাঝে
> প্রতিটার পর থেমে pass/fail-এর অপেক্ষা করবে না** — কারণ ব্যবহারকারী প্রতিটা সেশন আলাদা করে
> বিল্ড/টেস্ট করে জানাতে পারবেন না। প্রতিটা সেশন শেষে (তাদের নিজেদের কনভেনশন অনুযায়ী) zip +
> `ADMIN_ROLE_PROFILE_PROGRESS.md`-এ এন্ট্রি ঠিকই দিতে হবে (checkpoint/rollback-এর জন্য), কিন্তু
> এরপর **থামবে না, পরের সেশনে চলে যাবে**। **সেশন ৬ শেষ হওয়ার পর থামবে** এবং ব্যবহারকারীর কাছ থেকে
> ১-৬ সবগুলো মিলিয়ে একটা সম্মিলিত pass/fail-এর অপেক্ষা করবে — সেই কনফার্মেশন না পাওয়া পর্যন্ত
> **সেশন ৭ শুরু হবে না**। সেশন ৭-এর ভেতরের সাব-স্টেপ (৭.০-৭.৯) এই নিয়মের ব্যতিক্রম — ওগুলো
> ঝুঁকিপূর্ণ (২৫টা বিদ্যমান স্ক্রিন স্পর্শ করে, রিগ্রেশনের সম্ভাবনা বেশি), তাই ওখানে আগের
> per-sub-step confirmation-ই বহাল থাকবে (নিচে ৭.০-৭.৯-এর বর্ণনায় অপরিবর্তিত)। ⚠️ ঝুঁকি: এক টানে
> ৬টা সেশন চললে কোনো একটাতে (ধরুন সেশন ৩) সমস্যা হলেও সেটা সেশন ৬ শেষ না হওয়া পর্যন্ত ধরা পড়বে
> না — বাগ এলে কোন সেশনে হয়েছে সেটা ট্রেস করা তুলনামূলক কঠিন হবে, এটা মেনে নিয়েই এই ক্যাডেন্স
> বেছে নেওয়া হয়েছে।

1. **সেশন ১:** DB migration — `admin_roles`, `admin_accounts`, `admin_audit_logs` alter, উপরের RPC
   গুলো, সুপার-অ্যাডমিন-অপরিবর্তনীয়তার DB-trigger, বিদ্যমান ক্রেডেনশিয়াল থেকে সুপার অ্যাডমিন সিড।
2. **সেশন ২:** রিয়েল মাল্টি-এডমিন লগইন ফ্লো (সিঙ্গেল-ক্রেডেনশিয়াল গেট প্রতিস্থাপন), `AdminSession`
   স্টেট হোল্ডার।
3. **সেশন ৩:** `AdminRoleManagementView` — রোল CRUD + পারমিশন ট্রি UI (মকআপ ১-এর রোল এডিটর অনুযায়ী),
   লাইভ প্রিভিউ প্রোডাকশনে থাকবে কিনা এই সেশনের শুরুতেই কনফার্ম।
4. **সেশন ৪:** এডমিন অ্যাকাউন্ট লিস্ট + active/inactive + flag/unflag UI, DB-গার্ডের সাথে ওয়্যার করা।
5. **সেশন ৫:** অ্যাক্টিভিটি লগ ট্যাব UI (সুপার-অনলি) + নাম-ভিত্তিক সার্চ/সিলেক্ট/ফিল্টার-বাটন;
   `log_admin_action` কল-সাইট আপডেট **ধাপে ধাপে, স্ক্রিন বাই স্ক্রিন** (একবারে সব না)।
6. **সেশন ৬:** `AdminProfileView` — ছবি আপলোড, নাম/পদবি/ইমেইল/bio এডিট, read-only পারমিশন চিপ, লগইন
   তথ্য, সেশন লিস্ট, 2FA প্লেসহোল্ডার, পাসওয়ার্ড পরিবর্তন (বিদ্যমান bcrypt-আপডেট RPC প্যাটার্নে)।
   - **✅ পরবর্তীতে কনফার্মড (ব্যবহারকারী, ২০২৬-০৯-২৫): ফ্ল্যাগড অ্যাডমিন নিজের প্রোফাইল (নাম/ছবি/
     পদবি/ইমেইল/bio) বদলাতে পারবে না।** সেশন ৬-এ এখনো সার্ভার এটা আটকায় না (`admin_profile_update`-এ
     flagged-self-guard নেই) — এই ফিক্স সেশন ৭.১-এর সাথে (একই `AdminSession.canAct`/flagged-লক
     ফাউন্ডেশন ওয়্যারিং-এর সময়) করা: `admin_profile_update` RPC-তে
     `if v_me.flagged and v_target = v_me.id then raise exception 'FLAGGED_SELF_EDIT_BLOCKED'`
     জাতীয় গার্ড + ক্লায়েন্ট-সাইডেও (`AdminProfileView.kt`) ফিল্ডগুলো flagged-নিজের ক্ষেত্রে
     disabled দেখানো। **পাসওয়ার্ড পরিবর্তন এই গার্ডের বাইরে** (আগের সিদ্ধান্ত অপরিবর্তিত — ফ্ল্যাগড
     অ্যাডমিনও নিজের পাসওয়ার্ড বদলাতে পারবে, নাহলে লকড-আউট রিকভারির কোনো পথ থাকে না)।
7. **সেশন ৭ — বিদ্যমান ২৫টা Admin স্ক্রিনে রিয়েল পারমিশন-গেটিং + ফ্ল্যাগড-অ্যাডমিন গ্লোবাল
   অ্যাকশন-লক রিট্রফিট।** এটাই সবচেয়ে বড় কাজ, তাই **একবারে না — নিচের সাব-স্টেপে ভাগ করে, প্রতিটা
   সাব-স্টেপ শেষে কনফার্মেশন নিয়ে পরেরটায়** (ঠিক `ADMIN_PANEL_LOADING_MASTER_PROMPT.md`-এর সেশন
   ২.২৯-৩১-এর "প্রথমে Users, তারপর KYC..." প্যাটার্নে)। প্রতিটা সাব-স্টেপে যে স্ক্রিন ছোঁয়া হচ্ছে,
   **সেই একই সেশনে** ওই স্ক্রিনের `log_admin_action` কল-সাইটগুলোতেও `p_admin_id`/`p_admin_name`
   যোগ করা হবে (আলাদা পাস না, একই ফাইল দুইবার না খুলে একবারেই) — নিচে বন্ধনীতে যেসব ফাইলে আগে থেকে
   `log_admin_action` কল আছে সেগুলো চিহ্নিত করা হলো (zip গ্রেপ-ভেরিফায়েড ম্যাপিং)।

   - **৭.০ — শেয়ার্ড ফাউন্ডেশন (বাকি সব সাব-স্টেপের নির্ভরতা, সবার আগে):** একটা কেন্দ্রীয়
     `AdminSession` composition local/helper — `canAct(actionKey: String): Boolean` (রোল পারমিশন
     আছে **এবং** `flagged == false`; `view` অ্যাকশনের জন্য flagged-চেক বাদ, view সবসময় allowed যদি
     view-পারমিশন থাকে — মকআপের আচরণ অনুযায়ী), আর একটা শেয়ার্ড "এই মেনুতে অ্যাক্সেস নেই" খালি-স্টেট
     কম্পোজেবল (মকআপ ১-এর `sim-locked` স্টাইলের প্রোডাকশন সংস্করণ)। এই একটা ইউটিলিটি বানিয়ে
     রিইউজ করা হবে ৭.১-৭.৮-এর প্রতিটাতে — প্রতি স্ক্রিনে আলাদা `if (isSuper || ...)` না লিখে।

   - **৭.১ — ইউজার ব্যবস্থাপনা গ্রুপ:** `AdminUserLookupView.kt` (ইনডেক্স ১৮), `AdminUsersView.kt`
     (৪, log_admin_action আছে), `AdminKycView.kt` (১, log_admin_action আছে),
     `AdminSolverQuotaView.kt` (১৯)। `AdminAuditLogView.kt` (১২) এই সাব-স্টেপে ছোঁয়া হবে না — সেটা
     সেশন ৫-এ ইতিমধ্যেই সুপার-অনলি হয়ে গেছে।
     - **৭.১.১ — `admin-reset-user-password` Edge Function নিরাপত্তা-ফাঁক ফিক্স (ব্যবহারকারী
       কনফার্মড, ২০২৬-০৯-২৫: "যেভাবে করলে নিরাপত্তা ঠিক থাকবে ও প্রফেশনাল সিস্টেম হবে" — প্রস্তাবিত
       প্রফেশনাল সমাধান, এই সাব-স্টেপের শুরুতে বাস্তবায়নের আগে একবার confirm করে নেওয়া):**
       Edge Function-এ `is_admin(auth.uid())`-এর পাশাপাশি **টার্গেট নিজে এডমিন কিনা তা চেক করা** —
       টার্গেট `user_id` যদি `admin_accounts`-এ থাকে, তাহলে caller **সুপার অ্যাডমিন না হলে
       রিজেক্ট** (`TARGET_IS_ADMIN` জাতীয় এরর) — সাধারণ এডমিন কখনো অন্য এডমিনের (এমনকি সুপারেরও)
       পাসওয়ার্ড রিসেট করতে পারবে না। টার্গেট সাধারণ ইউজার হলে normal path — কিন্তু caller-এর
       `users:users:reset_password` পারমিশন (রোল থেকে) + flagged-না থাকাও চেক করা (৭.০-এর
       `canAct` ইউটিলিটি দিয়েই, ক্লায়েন্ট-সাইড গেট তো থাকবেই, কিন্তু Edge Function নিজেও
       independently verify করা — শুধু ক্লায়েন্ট-গেটের ওপর ভরসা না করে, যেহেতু service-role
       নিয়ে সরাসরি Edge Function কল করাও টেকনিক্যালি সম্ভব)।
     - **৭.১.২ — `AdminUsersView.kt`-এর রোল-চেঞ্জ ডায়ালগে "ADMIN" অপশনের বদলে "বানাও অ্যাডমিন" ফ্লো
       (বাগ-ফিক্স + নতুন ফিচার, ব্যবহারকারী কনফার্মড ২০২৬-০৯-২৫):**
       - **যে গ্যাপ ধরা পড়েছে:** Usergon মেনুতে বর্তমান রোল-চেঞ্জ ডায়ালগে USER/SOLVER-এর পাশে "ADMIN"ও
         একটা রেডিও-অপশন — বাছলে `admin_change_role` RPC শুধু লেগ্যাসি `public.users.role = 'ADMIN'`
         সেট করে দেয়। এতে পুরনো `is_admin(uid)` ফাংশন সত্যিই true হয়ে যায় (তাই পুরনো ২৫টা admin
         RPC-এর কিছু অ্যাকশনে কাজ করে), **কিন্তু** কোনো `admin_accounts` রো তৈরি হয় না — তাই সেশন ২-এর
         আসল মাল্টি-অ্যাডমিন লগইন (`admin_me`/`admin_session_start`) ডাকলে `NOT_AN_ADMIN` এরর, ব্যক্তি
         নতুন অ্যাডমিন প্যানেলে ঢুকতেই পারে না, কোনো রোল/পারমিশন অ্যাসাইন হয় না, "এডমিন অ্যাকাউন্ট"
         স্ক্রিনেও দেখায় না। সংক্ষেপে UI-তে "Admin" ব্যাজ দেখায় কিন্তু বাস্তবে ব্যবহারযোগ্য অ্যাডমিন হয় না।
       - **সিদ্ধান্ত (ব্যবহারকারী কনফার্মড, দুটো বিকল্প থেকে "পথ ২" — আলাদা লগইন, নিরাপদ):** বিদ্যমান
         `admin_account_create` RPC-এর `PHONE_IN_USE_BY_USER` গার্ড (সেশন ৪, ইচ্ছাকৃত — "নইলে কারো
         বিদ্যমান অ্যাকাউন্ট চুপচাপ এডমিনে রূপান্তর হয়ে যেত") **অক্ষত থাকবে, কোনো DB/RPC পরিবর্তন
         লাগবে না**। টার্গেট ইউজারের জন্য **একদম নতুন, আলাদা** এডমিন অ্যাকাউন্ট (নতুন `auth.users`
         আইডেন্টিটি, নতুন ফোন নম্বর + নতুন পাসওয়ার্ড আবশ্যক — পুরনো ফোনে করা যাবে না) তৈরি হবে; পুরনো
         USER/SOLVER অ্যাকাউন্টের row (`public.users`) **সম্পূর্ণ অক্ষত/অস্পৃষ্ট** থাকবে — balance,
         reputation, active jobs, escrow, KYC, `has_user_role`/`has_solver_role` ফ্ল্যাগ, কিছুই
         বদলাবে/মুছবে না। ফলে এই ব্যক্তির দুটো আলাদা লগইন হবে (এক ফোনে সাধারণ ইউজার/সলভার, আরেক ফোনে
         এডমিন) — সম্পূর্ণ আলাদা ডেটা, কোনো মিশ্রণ নেই, self-review/conflict-of-interest ঝুঁকিও নেই।
       - **পদবি vs রোল — দুটো আলাদা জিনিস (ব্যবহারকারীর প্রশ্নে কনফার্মড, ২০২৬-০৯-২৫):**
         - **পদবি (`designation`)** — `admin_accounts`-এর ফ্রি-টেক্সট ডিসপ্লে-লেবেল, কোনো পারমিশনের
           সাথে যুক্ত না। **সংশোধনী:** `public.users`/`UserEntity`-তে কোনো "পদবি" কলামই নেই (সেটা
           শুধু `admin_accounts`-এ আছে), তাই Usergon থেকে prefill করার মতো উৎস-ডেটা নেই — ফর্মে এই
           ফিল্ড **খালি থাকবে, ফ্রি-টেক্সট, ম্যানুয়ালি টাইপ করতে হবে** (আগের খসড়ায় ভুলভাবে "পদবি
           প্রি-ফিল" লেখা হয়েছিল, এখানে সংশোধন করা হলো)।
         - **রোল (`role_id`)** — আসল পারমিশন-গেটিং জিনিস, Role Management স্ক্রিনে যত রোল তৈরি করা
           আছে (যেমন কেউ সত্যিই "KYC ম্যানেজার" নামে রোল বানিয়ে নির্দিষ্ট পারমিশন সেট করলে) তার
           ড্রপডাউন-তালিকা থেকে **explicit select বাধ্যতামূলক** — বিদ্যমান ফর্মের নিয়মেই, কোনো
           prefill/preselect হবে না।
       - **স্কোপ (UI + in-place bottom-sheet ওয়্যারিং, কোনো নতুন migration/RPC না — বিদ্যমান
         `admin_account_create` পুনর্ব্যবহার):**
         1. রোল-চেঞ্জ ডায়ালগ থেকে "ADMIN" রেডিও-অপশন সরানো (শুধু USER/SOLVER থাকবে — legacy টগল,
            যেটা আসলে ঠিকঠাক কাজ করে, সেটা অপরিবর্তিত)।
         2. প্রতিটা নন-অ্যাডমিন ইউজার-কার্ডের মেনুতে নতুন অ্যাকশন **"বানাও অ্যাডমিন"** (শুধু
            `role != "ADMIN"` ইউজারদের জন্য visible, এবং শুধু caller নিজে **সুপার অ্যাডমিন** হলে —
            নিচের পারমিশন-গেটিং বুলেট দ্রষ্টব্য)।
         3. **নেভিগেশন — ব্যবহারকারী কনফার্মড (২০২৬-০৯-২৫): ট্যাব ২৬-এ নেভিগেট না, একই পেজে
            (`AdminUsersView.kt`) in-place বটম-শিট পপআপ** — ঠিক বিদ্যমান রোল-চেঞ্জ ডায়ালগের মতোই
            `BottomSlideAlertDialog` কম্পোনেন্ট ব্যবহার করে (যে ডিজাইন-প্যাটার্নে রোল-চেঞ্জ ডায়ালগ
            আগে থেকেই বানানো, সেই একই বটম-শিট স্টাইল)। `AdminAccountsView.kt`-এর তৈরির ফর্মের
            ফিল্ড-সেট/ভ্যালিডেশন লজিক পুনর্ব্যবহার হবে (কপি-পেস্ট এড়াতে শেয়ার্ড কম্পোজেবল/ফাংশন বের
            করে আনা, ট্যাব ২৬-এও একই কম্পোজেবল কল হবে) — কিন্তু কন্টেইনার/এন্ট্রি-পয়েন্ট আলাদা: এখানে
            bottom sheet popup, ওখানে ফুল-পেজ ফর্ম। প্রি-ফিল: শুধু **নাম + ইমেইল** (উপরের সংশোধনী
            অনুযায়ী, পদবি না); ফোন ও পাসওয়ার্ড ফিল্ড ফাঁকা (নতুন দিতে হবে); রোল explicit select।
         4. সফল হলে (RPC থেকে account row ফেরত) bottom sheet বন্ধ হয়ে টোস্ট + দুই জায়গার তালিকাই
            রিফ্রেশ (Usergon-এর ইউজার-লিস্ট অপরিবর্তিত থাকবে যেহেতু টার্গেট user row স্পর্শ হয়নি;
            "এডমিন অ্যাকাউন্ট" ট্যাব ২৬-এর তালিকায় নতুন এডমিন যোগ হবে)।
       - **পারমিশন-গেটিং — "বানাও অ্যাডমিন" বাটন নিজেও ৭.০-এর `canAct` ফাউন্ডেশন দিয়ে গেট হবে
         (ব্যবহারকারীর সাধারণ নীতি, ২০২৬-০৯-২৫, ইতিমধ্যে ৭.০-৭.৯-এর মূল ডিজাইনেই আছে — এখানে স্পষ্ট
         করে লেখা হলো):** ২৫টা স্ক্রিনের যেকোনো অ্যাকশনের মতোই — caller-এর রোলে সংশ্লিষ্ট পারমিশন
         (`users:users:create_admin` জাতীয় নতুন কী, ক্যাটালগে যোগ করতে হবে) টিক না থাকলে বাটনটাই
         **দেখানো হবে না** (grey-out/disabled না, একদম hidden — মকআপের কনভেনশন অনুযায়ী)। যেহেতু
         `admin_account_create` RPC নিজেই সার্ভার-সাইডে শুধু সুপার-অ্যাডমিন caller accept করে
         (`_admin_require_super()`), তাই এই অ্যাকশনটাকে ৭.০-এর **hard-super-only** তালিকায়ও যোগ করা
         হবে (`ADMIN_HARD_SUPER_ONLY_EXACT_KEYS`-এ) — ক্লায়েন্ট UI ও সার্ভার নিয়ম মিলে থাকবে, রোলে
         টিক করা থাকলেও নন-সুপারের জন্য বাটন hidden।
       - **সুপার অ্যাডমিন — কখনো তৈরি/অ্যাসাইন করা যাবে না (ব্যবহারকারীর গুরুত্বপূর্ণ কনফার্মেশন,
         ২০২৬-০৯-২৫; ইতিমধ্যেই DB-লেভেলে গ্যারান্টিড, নতুন কোনো কোড লাগবে না — শুধু ডকুমেন্টেশনে
         স্পষ্ট করে রাখা হলো):** কোনো ইউজার/সলভার/বিদ্যমান-নন-সুপার-এডমিন — কাউকেই "বানাও অ্যাডমিন"
         (বা রোল-ম্যানেজমেন্টের কোনো ফ্লো) দিয়ে সুপার অ্যাডমিন বানানো যাবে না, সিস্টেমে **সবসময়
         ঠিক ১ জনই** সুপার অ্যাডমিন থাকবে। ইতিমধ্যে ৩ স্তরে গার্ডেড (সেশন ১, ৪ — zip গ্রেপ-ভেরিফায়েড):
         1. `admin_account_create` RPC: `v_role.is_super` হলে `CANNOT_ASSIGN_SUPER_ROLE` রেইজ করে —
            তাই "বানাও অ্যাডমিন" ফর্মের রোল ড্রপডাউনে সুপার রোল বেছে সাবমিট করলেও সার্ভার ব্লক করবে।
         2. `admin_account_set_role` RPC: একই `CANNOT_ASSIGN_SUPER_ROLE` (কাউকে পরে সুপারে আপগ্রেড
            করা যাবে না) + `SUPER_ADMIN_IMMUTABLE` (বিদ্যমান সুপারের রোল কেউ বদলাতেও পারবে না)।
         3. `admin_role_upsert` RPC: নতুন কোনো রোল তৈরির সময় `is_super` প্যারামিটারই নেয় না (টেবিলের
            ডিফল্ট `false`) — তাই Role Management UI দিয়ে দ্বিতীয় কোনো "is_super" রোলও বানানো অসম্ভব।
         এই সাব-স্টেপে ক্লায়েন্ট-সাইডে অতিরিক্ত যা করণীয়: "বানাও অ্যাডমিন" ফর্মের রোল-ড্রপডাউনে
         সুপার রোলটা **তালিকা থেকেই বাদ** রাখা (UX ভালো করতে — সাবমিট করে সার্ভার-এরর খাওয়ার বদলে
         শুরুতেই না দেখানো), ঠিক যেমন বিদ্যমান `AdminAccountsView.kt`-এর তৈরির ফর্মেও ইতিমধ্যে আছে
         (সেশন ৪-এ "রোল স্পষ্টভাবে বাছতে হয়, প্রি-সিলেক্টেড না" — সুপার রোল ওখানেও তালিকায় নেই বলে
         ধরে নেওয়া হচ্ছে, বাস্তবায়নের শুরুতে যাচাই করে নেওয়া)।
       - **এই সাব-স্টেপে কী বদলাচ্ছে না:** `admin_change_role`/legacy USER↔SOLVER টগল ফাংশনালিটি
         অপরিবর্তিত থাকবে (সেটা ঠিকই কাজ করে); `admin_account_create`/`admin_account_set_role`/
         `admin_role_upsert` RPC-তে কোনো পরিবর্তন লাগবে না (সুপার-গার্ড আগে থেকেই আছে)।
   - **৭.২ — আর্থিক ব্যবস্থাপনা গ্রুপ (সবচেয়ে সংবেদনশীল, তাই ৭.১-এর পরপরই):**
     `AdminWithdrawalsView.kt` (২, log_admin_action আছে), `AdminEscrowView.kt` (৭),
     `AdminAdditionalChargesView.kt` (৮), `AdminTransactionsView.kt` (৯, বেশিরভাগ read-only হতে
     পারে — verify), `AdminGatewayPaymentsView.kt` (২৩, সম্ভবত read-only)।
     - **৭.২.১ — নতুন বিজনেস-রুল: PENDING extra bill থাকা অবস্থায় রিলিজ/স্প্লিট ব্লক +
       ডিসপিউট-পথে admin-এর নিজে Accept/Cancel করার ক্ষমতা (ব্যবহারকারী কনফার্মড, ২০২৬-০৯-২৫,
       এটা শুধু পারমিশন-গেটিং না, নতুন ফিচার/গার্ড — তাই ৭.২-এর মূল রিট্রফিটের সাথে মিশিয়ে না ফেলে
       এই সেশনেই আলাদা সাব-স্টেপ হিসেবে করা):**
       - **(ক) সলভার-সাইড রিলিজ-রিকোয়েস্ট গার্ড (admin panel-এর বাইরে, ইউজার/সলভার অ্যাপ-সাইড কোড —
         এই সাব-স্টেপেই একসাথে করা, আলাদা সেশনে ফেলে রাখা যাবে না):** যেই `additional_charges` রো
         এখনো `PENDING` (ইউজার accept/cancel কিছুই করেনি), সেই সমস্যার জন্য সলভার "কাজ সম্পন্ন/রিলিজ
         রিকোয়েস্ট" ফ্লো ট্রিগার করতে পারবে না। খুঁজে বের করা দরকার এই রিকোয়েস্ট ঠিক কোন
         ফাংশন/RPC/বাটন দিয়ে শুরু হয় (সম্ভবত সলভার-সাইড `Complete`/রিলিজ-রিকোয়েস্ট কল-সাইট,
         `confirmReleaseAndComplete`-এর আগের ধাপ) — client-সাইড গার্ড + সম্ভব হলে সংশ্লিষ্ট RPC-তেও
         সার্ভার-সাইড চেক (PENDING extra charge থাকলে ব্লক), যাতে শুধু UI-গার্ড দিয়ে বাইপাস করা না যায়।
       - **(খ) `AdminDisputeCenterView.kt`-এ নতুন Accept/Cancel বাটন:** কোনো সমস্যার dispute open
         থাকা অবস্থায় যদি সেই সমস্যার কোনো `additional_charges` রো `PENDING` থাকে, তাহলে admin
         নিজেই (ইউজারের বদলে) সেই extra bill-টা Accept বা Cancel করতে পারবে — নতুন RPC/UI প্রয়োজন
         (বিদ্যমান `respond_additional_charge`-এর admin-caller ভ্যারিয়েন্ট, অথবা নতুন
         `admin_respond_additional_charge`)। `log_admin_action`-এও এন্ট্রি যাবে (এই অ্যাকশনটা
         সেশন ১-এর সার্ভার-সাইড auto-identity প্যাটার্নেই স্বয়ংক্রিয়ভাবে কভার হবে)।
       - **(গ) Release/Split গেটিং:** Dispute-এ release বা split বাটন **ততক্ষণ enabled/কার্যকর হবে
         না** যতক্ষণ ওই সমস্যার সব `PENDING` extra bill (খ)-এর মাধ্যমে resolve (accept/cancel) না
         হয়েছে — (খ)-এর আগে (গ) করতে দেওয়া যাবে না, UI-তে বার্তাসহ ব্লক দেখানো।
       - **(ঘ) Refund অপ্রভাবিত:** Refund flow-এ extra bill-এর কোনো ভূমিকা নেই বলে এই গার্ড refund-এ
         **প্রযোজ্য না** — admin আগের মতোই normally refund করতে পারবে, PENDING extra bill থাকুক বা
         না থাকুক।
       - **সিদ্ধান্ত-বাতিল (ধাপ ২-এর আগের প্রশ্নের ফলাফল):** `finance:extra_charges:mark_settled`
         পারমিশন ক্যাটালগ থেকে **বাদ** — `mark_additional_charge_settled` RPC সবসময় অন্য একটা
         অ্যাকশনের (ইউজারের নিজের Confirm & Complete, অথবা (খ)/(গ)-এর dispute-resolve) সাইড-ইফেক্ট
         হিসেবেই অটোমেটিক কল হয়, কোনো এডমিন কখনো সরাসরি এই বাটনে চাপবে না — তাই আলাদা admin-action
         permission হিসেবে গেট করার দরকার নেই।
   - **৭.৩ — কাজ ও চুক্তি গ্রুপ:** `AdminProblemsView.kt` (৫, log_admin_action আছে),
     `AdminInstantJobsView.kt` (২১), `AdminCancelledBidsView.kt` (১৪, সম্ভবত read-only history —
     verify), `AdminDirectContractsView.kt` (১৬), `AdminChatMonitoringView.kt` (১৫)।
   - **৭.৪ — কনফিগারেশন গ্রুপ:** `AdminCategoriesView.kt` (৬), `AdminReputationEngineView.kt` (২০),
     `AdminSettingsView.kt` (১১ — সতর্কতা: এখানে বর্তমান single-admin phone/password change UI
     থাকতে পারে, সেশন ২-এর multi-admin migration-এর সাথে সাংঘর্ষিক না হয় সেটা এই সাব-স্টেপের শুরুতে
     আগে verify করা), `AdminFaqManagementView.kt` (১৩)।
   - **৭.৫ — বিরোধ ব্যবস্থাপনা + যোগাযোগ + মডারেশন গ্রুপ:** `AdminDisputeCenterView.kt` (২২),
     `AdminManualNotificationView.kt` (৩), `AdminRatingsView.kt` (১০, রিভিউ মডারেশন ট্যাব — নাম
     ভ্রান্তিকর, ফাইলনেম Ratings কিন্তু ড্রয়ারে "রিভিউ মডারেশন")।
   - **৭.৬ — সিস্টেম/অ্যাডভান্সড গ্রুপ:** `AdminSupabaseExplorerView.kt` (১৭),
     `AdminRefundDebugView.kt` (২৪) — দুটোই স্বভাবতই high-risk/debug টুল, ✅ **কনফার্মড
     (ব্যবহারকারী, ২০২৬-০৯-২৫): শুধু সুপার অ্যাডমিন** (গ্র্যানুলার পারমিশন বানানো হবে না, সরাসরি
     `isSuper` হার্ড-চেক)। একই সিদ্ধান্ত `config:settings:factory_reset` ও
     `config:settings:cleanup_commission`-এও প্রযোজ্য (৭.৪-এ, `AdminSettingsView.kt`-এ) — এই
     দুটোও গ্র্যানুলার পারমিশনের বদলে সরাসরি `isSuper` হার্ড-চেক দিয়ে গেট হবে।
   - **৭.৭ — ড্যাশবোর্ড:** `AdminStatsView.kt` (০) — সম্ভবত পুরোপুরি read-only (কোনো অ্যাকশন বাটন
     নেই), তাই শুধু `view` পারমিশন-চেক যথেষ্ট হতে পারে; স্ক্রিনে সত্যিই কোনো মিউটেটিং বাটন আছে কিনা
     আগে verify করে নেওয়া।
   - **৭.৮ — `AdminRatingsView.kt`-এর মতো drawer-এ নেই এমন কোনো sub-screen বাকি থাকলে (যেমন
     `AdminUsersView`-এর `onNavigateToReputation` থেকে পৌঁছানো `ReputationDetailScreen`-জাতীয়
     নেস্টেড স্ক্রিন) সেগুলোও এই ধাপে গ্রেপ করে আলাদা তালিকা করা, উপরের ৭.১-৭.৭-এ অন্তর্ভুক্ত না
     থাকলে বাদ পড়ে যেতে পারে।
   - **৭.৯ — শেষ QA পাস (সব সাব-স্টেপ শেষে):** একটা ফ্ল্যাগড, নন-সুপার টেস্ট অ্যাকাউন্ট দিয়ে পুরো
     অ্যাপ ঘুরে যাচাই করা যে **প্রতিটা** স্ক্রিনে সব অ্যাকশন বাটন সত্যিই ব্লকড (শুধু view কাজ করছে) —
     মকআপ ১-এর ফ্ল্যাগ-ফিচারের আসল, ক্রস-স্ক্রিন গ্যারান্টি এখানেই confirm হয়।

8. **সেশন ৮ (সেশন ৭ শেষ হওয়ার পর, ব্যবহারকারীর রিয়েল-ডিভাইস টেস্টে ধরা পড়েছে ২০২৬-০৯-২৫) — এডমিন
   ফোন নম্বর এডিট গ্যাপ ফিক্স:** এই মুহূর্তে `admin_accounts`-এর `phone` শুধু তৈরির সময় সেট হয়
   (`admin_account_create`) — এরপর কোনো এডিট RPC নেই। `AdminProfileView.kt`-এ ফোন ফিল্ড সবসময়
   `enabled = false` + "সুপার অ্যাডমিনের সাথে যোগাযোগ করুন" সাপোর্টিং-টেক্সট দেখায়, শর্তহীনভাবে —
   অথচ সুপার অ্যাডমিনও এখন কোথাও কারো ফোন বদলাতে পারেন না, তাই এই মেসেজটা dead-end। ফিক্স:
   - নতুন RPC `admin_account_update_phone(p_target_id, p_new_phone)` — শুধু সুপার-অ্যাডমিন গার্ড +
     ইউনিক-ফোন ভ্যালিডেশন (`admin_account_create`-এর প্যাটার্নে), সুপার নিজের ফোনও এভাবে বদলাতে
     পারবে কিনা confirm করা দরকার (নাকি সুপারেরটা একদমই লকড থাকবে, শুধু নন-সুপারদেরটা সুপার বদলাতে
     পারবে — এই সাব-প্রশ্নটাও সেশন ৮ শুরুর আগে কনফার্ম করা)।
   - `AdminProfileView.kt`-এ ফোন ফিল্ড শর্তসাপেক্ষে এডিটেবল: `canEdit && isSuper && !isSelf`
     (প্রস্তাবিত — উপরের সাব-প্রশ্নের উত্তরের ওপর নির্ভর করে বদলাতে পারে) হলে এডিট-অ্যাবল, নাহলে
     বর্তমান লক-করা অবস্থাই থাকবে।

9. **সেশন ৯ (সেশন ৭ শেষ হওয়ার পর, ব্যবহারকারীর অনুরোধ ২০২৬-০৯-২৫) — কোল্ড-স্টার্ট সেশন-পার্সিস্টেন্স +
   রিফ্রেশ:** ডিভাইসে যে এডমিন/রোল দিয়ে সর্বশেষ লগইন করা ছিল, অ্যাপ কিল/ক্লোজ করলে **লগআউট হবে না** —
   সেশন ক্যাশে/পার্সিস্ট থাকবে, আর অ্যাপ আবার খুললেই সব ডেটা (রিয়েল-টাইম ফিচারসহ) নতুন করে
   আপডেট/সিঙ্ক হয়ে যাবে (আলাদা করে আবার লগইন করা লাগবে না)।
   - **বর্তমান অবস্থা যাচাই করা প্রথম কাজ (অনুমান না):** Supabase Auth সেশন (সেশন ২-এর real sign-in)
     সম্ভবত ডিফল্টভাবেই ডিভাইসে পার্সিস্ট থাকে (Supabase Kotlin SDK-র সাধারণ আচরণ), কিন্তু
     `AdminSession` অবজেক্ট (রোল/পারমিশন/ফ্ল্যাগ ধরে রাখা `StateFlow`) সম্পূর্ণ **in-memory** —
     অ্যাপ প্রসেস কিল হলে এটা হারিয়ে যায়। তাই আসল প্রশ্ন: প্রসেস-কিলের পর অ্যাপ আবার খুললে এখন কী
     হয় — জোর করে লগইন স্ক্রিনে পাঠায়, নাকি স্প্ল্যাশ/লোডিং স্টেটে আটকে থাকে? এটা কোড-গ্রেপ +
     ডিভাইস-টেস্ট দুটো দিয়েই কনফার্ম করে শুরু করা।
   - **কোল্ড-স্টার্ট রিস্টোর:** অ্যাপ চালু হওয়ার সময় Supabase Auth-এ ভ্যালিড পার্সিস্টেড সেশন থাকলে
     (ও সেটা এডমিন হলে), সরাসরি লগইন স্ক্রিন না দেখিয়ে `admin_me`/সমতুল্য RPC কল করে
     `AdminSession.set(...)` দিয়ে স্টেট রিস্টোর করা, তারপরই এডমিন প্যানেল দেখানো।
   - **ডেটা + রিয়েল-টাইম রিফ্রেশ:** রিস্টোরের পরপরই বর্তমানে খোলা ট্যাবের ডেটা রিফ্রেচ করা এবং
     রিয়েল-টাইম subscription/channel-গুলো (যেগুলো প্রসেস-কিলে বন্ধ হয়ে গিয়েছিল) নতুন করে সাবস্ক্রাইব
     করা — যাতে ব্যবহারকারী মনে করেন কিছুই বন্ধ হয়নি।
   - **হার্ড গার্ড অক্ষত থাকা জরুরি:** সেশন ২-এর heartbeat-ভিত্তিক জোর-লগআউট (`ACCOUNT_INACTIVE`/
     `NOT_AN_ADMIN`/ফ্ল্যাগড-হলে পারমিশন-রিফ্রেশ) কোল্ড-স্টার্ট রিস্টোরের পরেও ঠিক আগের মতোই কাজ
     করতে হবে — অর্থাৎ "লগআউট হবে না" মানে শুধু স্বাভাবিক অ্যাপ-কিল/রিওপেনের ক্ষেত্রে; অ্যাকাউন্ট
     সত্যিই নিষ্ক্রিয়/ডিলিটেড/আনফ্ল্যাগ-এর বদলে ফ্ল্যাগড হলে সেই জোর-লগআউট/রিফ্রেশ বহাল থাকবে।

---

## ধাপ ৫ — কাজ শুরুর আগে ব্যবহারকারীর কাছ থেকে যা কনফার্ম করা দরকার

1. মাল্টি-এডমিন রিয়েল লগইন (সেশন ১-২, ব্রেকিং চেঞ্জ) কি এখনই শুরু হবে, নাকি প্রথমে শুধু
   UI/স্কিমা রেডি করে লগইন-প্রতিস্থাপন পরে আলাদাভাবে করব?
2. মকআপ ১-এর ডানপাশের লাইভ প্রিভিউ সিমুলেটর প্রোডাকশনে সুপার অ্যাডমিনের জন্য রাখা হবে, নাকি শুধু
   রোল-বানানোর সময়ের ডিজাইন-টুল?
3. "সাম্প্রতিক সেশন" ও "অনলাইন" স্ট্যাটাস — সত্যিকারের রিয়েল-টাইম প্রেজেন্স (heartbeat/presence
   channel) দরকার, নাকি শুধু `last_login_at` দিয়ে approximate রাখব (v1-এর জন্য অনেক সহজ)?
4. অ্যাক্টিভিটি লগে "কারণ" আলাদা কলাম হবে, নাকি বিদ্যমান `details` ফিল্ডেই ফরম্যাট করে রাখব
   (migration কম লাগবে)?
5. ফ্ল্যাগ ফিচার active/inactive-এর পাশে আলাদা বুলিয়ান থাকবে (মকআপের মতো), নাকি একটাই
   status enum (`active` / `inactive` / `flagged`)-এ একীভূত হবে?
6. প্রোফাইল — production-এ প্রতিটা এডমিন **শুধু নিজের** প্রোফাইল দেখবে (সুপার অ্যাডমিন হয়তো সবার
   দেখতে/এডিট করতে পারবে), নাকি মকআপের মতো সবাই সবারটা দেখতে পারবে (শুধু নিজেরটা এডিট করতে পারবে)?
7. পাসওয়ার্ড পরিবর্তন — বিদ্যমান `admin_credentials_update`-এর প্যাটার্নে (current-password
   re-verify) হবে, নাকি per-account আলাদা RPC/ফ্লো লাগবে?
8. "প্রোফাইল" নেভিগেশন এন্ট্রি কোথায় বসবে — ড্রয়ারের "অ্যাডমিন ব্যবস্থাপনা" গ্রুপে (তাহলে নন-সুপার
   এডমিনরা কীভাবে নিজের প্রোফাইলে পৌঁছাবে, যেহেতু ওই গ্রুপ সুপার-অনলি), নাকি টপ বার/প্রোফাইল
   আইকনে সবার জন্য সবসময়-দৃশ্যমান জায়গায়?

---

**রেফারেন্স ফাইল:** `admin-role-management.html`, `admin-profile.html` (এই চ্যাটে আগে বানানো, একই
সেশনে ডাউনলোড করা আছে) — যেকোনো সেশন শুরুর আগে ব্রাউজারে খুলে ইন্টারঅ্যাকশন/ফিল্ড লিস্ট নিজে দেখে নেওয়া
বাধ্যতামূলক, শুধু এই ফাইলের বুলেট-সামারি ভরসা না করে।
