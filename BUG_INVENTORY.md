# 🐛 Somadhan Bug Inventory — CI_TEST_SUITE (Step 0–20) থেকে consolidated

এই ফাইল `CI_TEST_SUITE_PROGRESS.md` (৯৭৭৯ লাইন, ৬৯টা "Step সম্পূর্ণ" এন্ট্রি) পুরোটা ঘেঁটে বানানো —
প্রতিটা আলাদা বাগকে এক জায়গায়, একটা status সহ এনেছে, যাতে fix করার সময় কোনোটা বাদ না পড়ে।
`SOMADHAN_BUG_FIX_MASTER_PROMPT.md`-এর প্রতিটা Step এই ফাইলের একটা নির্দিষ্ট সারি/গ্রুপকে টার্গেট করে
— fix হয়ে গেলে সেই সারির Status এখানে আপডেট হবে (fix master prompt নিজের rule-এ এটা বলা আছে)।

**Legend:**
- ✅ **FIXED + VERIFIED** — কোড ফিক্স হয়েছে, Windows real-run দিয়ে pass কনফার্মড।
- 🟡 **FIXED, NOT VERIFIED** — কোড ফিক্স হয়েছে কিন্তু কোনো real Windows-run কখনো confirm করেনি।
- 🔴 **NOT FIXED, TEST EXISTS** — বাগ প্রমাণ করার একটা ইচ্ছাকৃতভাবে-fail-করা টেস্ট আছে, কিন্তু কোড এখনো ঠিক হয়নি।
- 🟠 **NOT FIXED, NO TEST** — শুধু investigation/root-cause report আছে, কোনো automated প্রমাণ নেই।
- 📝 **BACKLOG / DECISION NEEDED** — বাগ ঠিক না, design/scope প্রশ্ন — user-এর সিদ্ধান্ত লাগবে আগে।
- ⚪ **NO BUG FOUND** — সন্দেহ করা হয়েছিল কিন্তু investigation-এ ক্লিন প্রমাণিত (রেফারেন্সের জন্য রাখা)।

---

## গ্রুপ ১ — 🔴 Account-switch / Realtime subscription gap (Step 18, Step 19-ক)

**এই একটাই root cause, কিন্তু একাধিক রিপোর্ট-করা উপসর্গ কভার করে — ইউজারের নিজের দেওয়া দুটো
উদাহরণই (bid-accept balance-reset, withdrawal balance-reset) এখানে পড়ে।**

| # | বাগ | ফাইল/ফাংশন | প্রমাণ (test) | Status |
|---|---|---|---|---|
| ১.১ | `logout()`/`completeLoginAfterOtp()`/`switchRoleToSolver()`/`switchRoleToUser()`/`loginAsAdmin()` — কোনোটাই `SupabaseRealtimeManager.startRealtimeListeners()`/`stopRealtimeListeners()` আবার কল করে না, ফলে `user:<uuid>` broadcast channel (balance/transactions/escrows/withdrawals/gateway_payments/additional_charges/notifications — ৭টা টেবিল) app-process-এর প্রথম account-এই আটকে থাকে account-switch-এর পরেও | `SomadhanViewModel.kt` (৫টা login-path ফাংশন), `SupabaseRealtimeManager.kt` (`attachDatabase`, `startUserTopicBroadcastSubscription`) | `RealtimeSubscriptionScopeTest.kt` (Step 18.3, ৪টা test) — কোড-ফিক্স হয়ে গেছে (Somadhan Bug-Fix Step 1), **Windows real-run দিয়ে pass কনফার্মড** (`.\gradlew.bat testDebugUnitTest --tests "*RealtimeSubscriptionScopeTest*"` → `BUILD SUCCESSFUL`, ব্যবহারকারীর মেশিনে, ২০২৬-০৯-২৩) | ✅ |
| ১.২ | **Step 1-এর regression, real device-এ ধরা পড়েছে (Somadhan Bug-Fix Step 6 testing, ২০২৬-০৯-২৩):** ১.১-এর ফিক্স-করা প্রতিটা caller (`completeLoginAfterOtp`/`switchRoleToSolver`/`switchRoleToUser`/`loginAsAdmin`) `_currentUser`/role আগে কমিট করে, **তারপর** `runCatching { startRealtimeListeners() }` কল করে, `onSuccess()/onReady()` তার পরে। কিন্তু `RealtimeChannel.subscribe()`-এর কোনো নিজস্ব timeout নেই — network/firewall/Supabase-side কোনো কারণে subscribe আটকে গেলে (internet থাকা অবস্থাতেও, ব্যবহারকারী-কনফার্মড) `runCatching` কোনো exception না পেয়ে suspend অবস্থাতেই আটকে থাকে, `onSuccess()/onReady()` কখনো কল হয় না — ফলে normal user/solver OTP-login, role-switch, admin-login সবগুলোতেই UI চিরকাল loading-এ আটকে যায় (যদিও state ইতিমধ্যে কমিট হয়ে গেছে — app kill+reopen করলে সেটা দেখা যায়) | `SupabaseRealtimeManager.kt` (`startRealtimeListeners()`-এর ১০টা `.subscribe()` কল + `startUserTopicBroadcastSubscription()`-এর ১টা) | কোনো automated test নেই (real network/WebSocket-নির্ভর, unit test likely mock করে তাই ধরা পড়েনি) — ফিক্স: নতুন `RealtimeChannel.subscribeWithTimeout()` হেল্পার (এই ফাইলের `pullBulkDataFromSupabase()`-এর `withTimeout(20_000L)` precedent অনুসরণ করে, ১০ সেকেন্ড টাইমআউট) দিয়ে সবগুলো `.subscribe()` (১১টা, শুধু login/role-switch path-এর ভেতরেরগুলো — চ্যাট/ডিসপিউট-নির্দিষ্ট আরও ৩টা `.subscribe()` [`joinProblemMessagesBroadcastChannel`/`joinProblemBidsBroadcastChannel`/আরেকটা] ইচ্ছাকৃতভাবে ছোঁয়া হয়নি, স্কোপের বাইরে, গ্রুপ ৭-এ নোট করা হলো) প্রতিস্থাপন করা হয়েছে — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ১.৩ | **১.১/১.২-এর থেকে আলাদা root cause, ব্যবহারকারী নিজের ডিভাইসে ১.১/১.২-এর ফিক্স manual-verify করতে গিয়ে ধরেছেন (২০২৬-০৯-২৩, "সমাধান হয়নি, wallet-এ 'কিছু লেনদেন cloud-এ sync হতে বাকি আছে' warning" রিপোর্ট)।** `completeLoginAfterOtp()` সবার আগে `repository.refreshUserDataFromCloud(user.id)` কল করে, যেটা Supabase থেকে user row টেনে **unconditionally** local balance/reputationScore/isBanned/isRestricted ওভাররাইট করে দিত (`cacheUserLocally()`)। কিন্তু escrow/wallet dual-write RPC (`addToEscrow`/`payoutEscrowToSolver`/`refundEscrowOnceLocked`) cloud-push ব্যর্থ হলে `enqueueOutboxRetry()` একটা `pending_sync_outbox` entry বানায় (`OutboxPendingIndicator`-এর warning এই entry-রই UI) — ঠিক সেই window-এ Supabase-এর balance এখনো stale, তাই account-switch (logout/login) করলে local (সঠিক, sync-pending) balance cloud-এর পুরনো মান দিয়ে চাপা পড়ে যাচ্ছিল | `SomadhanRepository.kt` (`refreshUserDataFromCloud()`, লাইন ~৬৯৬) | কোনো automated test নেই (Room+outbox+network টাইমিং-নির্ভর, ১.২-এর মতোই real-device-only reproduce হয়) — ফিক্স: `refreshUserDataFromCloud()`-এ `pendingSyncOutboxDao.getPending()` চেক করে, pending entry থাকলে balance/reputationScore/isBanned/isRestricted local-ই রাখা হচ্ছে (cloud overwrite skip), বাকি profile/KYC ফিল্ড আগের মতোই cloud থেকে আপডেট হয় — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ১.৪ | **১.২-এর নিজেরই regression, ব্যবহারকারী real device-এ ১.২-এর ফিক্স verify করতে গিয়ে ধরেছেন (২০২৬-০৯-২৩, "৩.১a/৩.১c-এর login issue এখনও fix হয়নি" রিপোর্ট)।** ১.২-এর ফিক্স (`subscribeWithTimeout()`, ১০ সেকেন্ড হার্ড টাইমআউট) প্রতিটা `.subscribe()` কলকে চিরকাল ঝোলা থেকে বাঁচালেও, `startRealtimeListeners()`-এর ১০টা `subscribeWithTimeout()` কল **sequentially (একটার পর একটা)** চলছিল — network/WebSocket সমস্যায় প্রতিটা নিজের পুরো ১০ সেকেন্ড একে একে ব্যবহার করলে মোট worst-case wait ~১০×১০=১০০ সেকেন্ড হয়ে যায়, যা ব্যবহারকারীর কাছে "এখনও আটকে থাকা/ভাঙা"-ই মনে হয় | `SupabaseRealtimeManager.kt` (`startRealtimeListeners()`, লাইন ~১৬৯২) | কোনো automated test নেই (network-timing-নির্ভর, ১.২-এর মতোই) — ফিক্স: ১০টা `subscribeWithTimeout()` কল `coroutineScope { launch { ... } }` দিয়ে **সমান্তরালে (parallel)** চালানো হচ্ছে, worst-case wait এখন ~১০ সেকেন্ডে সীমাবদ্ধ (১০০ না); `startUserTopicBroadcastSubscription()`-এর নিজস্ব ১টা subscribe এখনও sequential রাখা হয়েছে (rule #৩, ন্যূনতম এডিট — এটা মাত্র ~১০ সেকেন্ড অতিরিক্ত যোগ করতে পারে) — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ১.৫ | **১.৩-এর ফিক্সের পরও balance বারবার "reset" হওয়ার আসল কারণ (ব্যবহারকারী রিপোর্ট: "কিছুই ঠিক হয়নি, balance আবার reset হয়ে যাচ্ছে")।** ১.৩ শুধু `SomadhanRepository.refreshUserDataFromCloud()`-এর একটা call-site প্রোটেক্ট করেছিল। কিন্তু সম্পূর্ণ আলাদা ফাইলে (`SupabaseRealtimeManager.kt`) থাকা `mergeAndSaveUser()` — যেটা তিনটা ভিন্ন caller (realtime `users`-table UPDATE event, `checkListenerHealthAndFallbackSync()`-এর fallback pull, `pullBulkDataFromSupabase()`-এর app-শুরুর bulk pull) থেকেই কল হয় — একটা আগের (money-flow ফিক্স ধাপ ১) ইচ্ছাকৃত রিমুভালের কারণে balance/status ফিল্ডের জন্য **কোনো গার্ড ছাড়াই** সরাসরি dto-এর মান নিত। ফলে ১.৩ লগইনের সময় ঠিক করে দেওয়ার পরপরই realtime subscription চালু হয়ে বা health-check/bulk-pull চলে আবার সেই একই stale cloud balance দিয়ে local overwrite করে দিত | `SupabaseRealtimeManager.kt` (`mergeAndSaveUser()`, লাইন ~৭৭৪, এবং তার caller `handleUserAction`/`checkListenerHealthAndFallbackSync`/`pullBulkDataFromSupabase`) | কোনো automated test নেই (network/realtime-timing-নির্ভর, ১.২/১.৩-এর মতোই) — ফিক্স: `mergeAndSaveUser()`-এ ১.৩-এর মতোই `pendingSyncOutboxDao.getPending()` চেক যোগ করা হয়েছে, pending entry থাকলে legacy (balance/reputationScore/isBanned/isRestricted) **ও** role-scoped mirror কলাম (balanceUser/balanceSolver/reputationScoreUser/_Solver/isBannedUser/_Solver/isRestrictedUser/_Solver) দুটোই local-ই রাখা হচ্ছে — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ১.৬ | **১.৩/১.৫-এর গার্ডও কেন যথেষ্ট ছিল না তার শেষ (এবং কালানুক্রমে সবচেয়ে আগে ঘটা) root cause।** প্রতিটা লগইন/account-switch-এর **Step 1** (`SomadhanViewModel.validateLoginCredentials()` → `SomadhanRepository.loginWithPhonePassword()`, OTP verify হওয়ার *আগেই* চলে) নিজেই unconditionally cloud থেকে user টেনে `cacheUserLocally()` দিয়ে local balance/status ওভাররাইট করত — ১.৩-এর `refreshUserDataFromCloud()` (Step 2, `completeLoginAfterOtp()`-এ) চলারও আগে। ফলে account-switch-এ Step 1-ই প্রথমে stale cloud balance দিয়ে local (সঠিক) balance চাপা দিয়ে দিত, আর তারপর Step 2-এর ১.৩ গার্ড যে `existingLocal` পড়ত সেটা ততক্ষণে ইতিমধ্যেই করাপ্টেড — তাই গার্ড থেকেও কোনো লাভ হচ্ছিল না | `SomadhanRepository.kt` (`loginWithPhonePassword()`, লাইন ~১১৮৫) | কোনো automated test নেই (একই কারণে) — ফিক্স: ১.৩/১.৫-এর মতোই pending outbox থাকলে legacy + role-scoped mirror দুটো ফিল্ড-সেটই local-ই রাখা হচ্ছে, `cacheUserLocally()` কল করার আগেই — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ১.৭ | **১.৩/১.৫/১.৬-এর গার্ড সব বসানোর পরও ব্যবহারকারীর নতুন repro-তে বাগ persist করেছে — সম্পূর্ণ ভিন্ন category-র root cause, Supabase MCP দিয়ে সরাসরি cloud DB verify করে ধরা পড়েছে (২০২৬-০৯-২৩): `escrows` টেবিলে সর্বমোট ০ row (জীবনেও কোনো escrow cloud-এ যায়নি), test user-এর cloud `balance`/`balance_user` কখনো কমেইনি।** ১.৩/১.৫/১.৬ সবগুলোই ধরে নিয়েছিল যে cloud-এর dual-write **সফল হয়েছে কিন্তু stale** — বাস্তবে এই repro-তে dual-write-ই কখনো ঘটেনি। `acceptBid()`-এ Supabase RPC call **এবং** ব্যর্থ হলে `enqueueOutboxRetry()` — দুটোই `if (SupabaseAuthManager.currentUserId() == problem.userId)`-এর ভেতরে বন্দী; শর্ত false হলে RPC call-ই হয় না, তাই `enqueueOutboxRetry()`-ও কখনো কল হয় না — transaction outbox-এও জমা হয় না, `OutboxPendingIndicator`-এও দেখা যায় না, চিরকাল unsynced থেকে যায়। শর্তটা false হওয়ার কারণ: `SomadhanViewModel.logout()` (ধাপ ১৪) ইচ্ছাকৃতভাবে `SupabaseAuthManager.signOut()`-কে fire-and-forget কোরুটিনে রাখে (অফলাইন logout আটকে না যাওয়ার জন্য) — দ্রুত অন্য account-এ login করলে সেই দেরি-হওয়া signOut() নতুন session তৈরির *পরে* ফায়ার হয়ে সেটাই মুছে দিতে পারত, ফলে ঠিক accept-bid-এর সময় `currentUserId()` null/mismatch হয়ে যেত | `SomadhanRepository.kt` (`acceptBid()`, লাইন ~৩০১৫ identity guard), `SomadhanViewModel.kt` (`logout()`, লাইন ~৩১১৪, fire-and-forget `signOut()` Job), `SupabaseAuthManager.kt` (`signInWithPhonePassword()`/`signOut()` — কোনো session-race protection ছিল না) | কোনো automated test নেই (auth-session-timing-নির্ভর, real network দরকার, ১.২/১.৪-এর মতোই ধরনের race) — ফিক্স: `SupabaseAuthManager`-এ নতুন `pendingSignOutJob` (Job?) ট্র্যাকার + `trackPendingSignOut()`/`awaitPendingSignOutIfAny()` যোগ করা হয়েছে; `logout()`-এর fire-and-forget Job এখন `trackPendingSignOut()`-এ রেজিস্টার হয়; `signInWithPhonePassword()` শুরুতেই bounded (৫ সেকেন্ড timeout) `awaitPendingSignOutIfAny()` কল করে, যাতে আগের account-এর দেরি-হওয়া signOut() নতুন session মুছে দিতে না পারে — offline logout তবুও সাথে সাথেই স্থানীয়ভাবে সম্পন্ন হয় (অপরিবর্তিত), শুধু *পরের* online login সামান্য (সাধারণত instant) অপেক্ষা করে — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |

| ১.৮ | **১.৭-এর ফিক্স থাকা সত্ত্বেও ব্যবহারকারীর real-device re-test-এ একই symptom persist করেছে — screenshot দিয়ে প্রমাণ করেছেন যে app-এর ভেতরে escrow/job এখনো live/active দেখাচ্ছে যদিও Supabase MCP দিয়ে verify করে cloud `escrows`-এ ০ row আর `balance`/`balance_user` অপরিবর্তিত (৭৫০) পাওয়া গেছে — অর্থাৎ ১.৭-এর হাইপোথিসিসে (`accept_bid` RPC কখনো cloud-এ পৌঁছায়নি) কিছু বদলায়নি, কিন্তু সেটা এই নির্দিষ্ট symptom (role switch করলেই balance ফিরে যাওয়া)-এর একমাত্র কারণ ছিল না — সম্পূর্ণ ভিন্ন, স্বতন্ত্র bug (root cause সম্পূর্ণ আলাদা কোড-পথ) ধরা পড়েছে কোড রিভিউ করে।** `switchRoleInPlace()` (User↔Solver role-switch-এর মূল ফাংশন)-এর নিজস্ব cloud-sync ব্লক (`SupabaseSyncManager.switchRole(...).onSuccess { ... }`) `switch_role_get_or_create_linked_profile` RPC-এর ফেরত দেওয়া `balance_user`/`balance_solver` (এই RPC নিজে সবসময় সফল হয় — `accept_bid`-এর identity-guard বাগে আটকায় না, শুধু cloud-এর মান-ই stale কারণ `accept_bid`-এর dual-write আগে থেকেই cloud-এ পৌঁছায়নি) দিয়ে সরাসরি, **কোনো pending-outbox গার্ড ছাড়াই** local balance/reputationScore/isBanned/isRestricted (legacy + role-scoped মিরর দুটোই) ওভাররাইট করে দিচ্ছিল। ১.৩/১.৫/১.৬-এর গার্ড অন্য তিনটা call-site (`refreshUserDataFromCloud`, `mergeAndSaveUser`, `loginWithPhonePassword`) প্রোটেক্ট করলেও, **role-switch-এর নিজস্ব এই চতুর্থ call-site কখনো ছোঁয়া হয়নি** — এটাই কেন role switch করা মাত্রই balance আবার stale cloud মানে (৭৫০) ফিরে যাচ্ছিল তার সরাসরি ব্যাখ্যা | `SomadhanRepository.kt` (`switchRoleInPlace()`, `SupabaseSyncManager.switchRole(...).onSuccess` ব্লক) | কোনো automated test যোগ করা হয়নি (১.৩/১.৫/১.৬-এর মতোই real Room+outbox+network timing-নির্ভর প্যাটার্ন অনুসরণ করা হলো) — ফিক্স: ১.৩/১.৫/১.৬-এর হুবহু একই `pendingSyncOutboxDao.getPending()` চেক এই ব্লকেও যোগ করা হয়েছে — pending entry থাকলে cloud-sync ব্লক পুরোটাই স্কিপ হয়ে যায় (`updatedRoot` local মানই থেকে যায়, `userDao.updateUser(cloudSynced)` কলই হয় না), নাহলে আগের মতোই cloud-sync হয় — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |

| ১.৯ | **সবচেয়ে গুরুত্বপূর্ণ, একত্রীকরণকারী root cause — ব্যবহারকারী নিজেই ধরিয়ে দিয়েছেন: "এটা শুধু role-switch-এর বিষয় ছিল না, logout করে আবার login করলেও একই সমস্যা হতো।"** ১.৩/১.৫/১.৬/১.৮ — এই চারটা গার্ডই `pendingSyncOutboxDao.getPending()`-এর উপর নির্ভরশীল, অর্থাৎ এরা ধরেই নেয় যে local balance cloud-এর চেয়ে এগিয়ে থাকলে `pending_sync_outbox`-এ একটা entry থাকবেই। কিন্তু `acceptBid()`-এর `if (currentUserId() == problem.userId) { ...RPC... } ` ব্লকের কোনো `else` শাখাই ছিল না — শর্ত false হলে (১.৭-এর বর্ণিত session-race অনুযায়ী) RPC কল, `enqueueOutboxRetry()`, এমনকি কোনো log-ও হতো না। ফলে identity-mismatch হলে `pending_sync_outbox`-এ **কখনোই কোনো entry তৈরি হতো না** — আর তাই ১.৩/১.৫/১.৬/১.৮-এর প্রতিটা গার্ডই "pending কিছু নেই" দেখে stale cloud balance দিয়ে local overwrite করার অনুমতি দিয়ে দিত, তা সেই overwrite login-এ ঘটুক (`refreshUserDataFromCloud`/`loginWithPhonePassword`) বা role-switch-এ (`switchRoleInPlace`, ১.৮) বা realtime merge-এ (`mergeAndSaveUser`, ১.৫) — চারটা path-ই একই root cause-এর শিকার | `SomadhanRepository.kt` (`acceptBid()`-এর RPC-guard ব্লক, `if (currentUserId() == problem.userId) {...}`-এর কোনো `else` ছিল না) | কোনো automated test যোগ করা হয়নি (১.৭-এর মতোই auth-session-timing-নির্ভর) — ফিক্স: এই `if`-এর একটা `else` শাখা যোগ করা হয়েছে যেটা identity-mismatch-কেও (ঠিক `onFailure`-এর মতোই shape-এর) একটা `enqueueOutboxRetry("accept_bid", ...)` কল হিসেবে গণ্য করে — এখন থেকে mismatch হলেও একটা outbox entry তৈরি হবে, তাই ১.৩/১.৫/১.৬/১.৮-এর সব গার্ডই সঠিকভাবে "pending sync আছে" দেখতে পাবে এবং local balance protect করবে, path (login/role-switch/realtime-merge) যাই হোক না কেন — real-device-এ ব্যবহারকারী কনফার্ম করেছেন সবকিছু normally কাজ করছে (২০২৬-০৯-২৪) | ✅ |

| ১.১০ | **১.২/১.৪-এর ফিক্স (timeout + parallel) থাকা সত্ত্বেও ব্যবহারকারী real-device-এ নতুন উপসর্গ রিপোর্ট করেছেন (২০২৬-০৯-২৪): "OTP verify করলে popup হারিয়ে যাচ্ছে, তারপর instant login/role-select popup আসতে অনেক সময় লাগছে, কোনো loading-ও দেখাচ্ছে না — মনে হচ্ছে app login হচ্ছে না। Admin-এর ক্ষেত্রেও same। Profile-এ role switch করলে অনেক বেশি সময় লাগছে, শুধু loading হচ্ছে, background-এ role switch হয়ে গেছে।"** কোড রিভিউ করে ধরা পড়েছে: ১.১-এর ফিক্স করা ৪টা caller-ই (`completeLoginAfterOtp`/`loginAsAdmin`/`switchRoleToSolver`/`switchRoleToUser`) `runCatching { SupabaseRealtimeManager.startRealtimeListeners() }` কে **await করে** (suspend call, blocking) — ১.৪-এর পর parallel হওয়ায় worst-case ~১০০ সেকেন্ড থেকে ~১০ সেকেন্ডে নামলেও, `onSuccess()/onReady()` তবুও সেই পুরো সময়টা আটকে থাকে, যদিও `_currentUser`/role/local DB ততক্ষণে already committed (realtime resubscribe শুধু live-update-এর জন্য, সঠিকতার জন্য না — non-blocking হলেও কোনো ক্ষতি নেই)। এর উপরে, `LoginScreen.kt`-এর OTP-verify confirm button আগে `verifyOtp()`-এর `onSuccess`-এই `isVerifyingLoginOtp = false` আর `showLoginOtpDialog = false` করে দিত (dialog বন্ধ), **তারপর** `completeLoginAfterOtp()` কল হতো — অর্থাৎ dialog বন্ধ হওয়া আর role-select popup/login-success আসার মাঝের পুরো gap-এ (network calls + realtime resubscribe) কোনো loading UI-ই ছিল না, যেটা ব্যবহারকারীর বর্ণনা করা "popup হারিয়ে যাচ্ছে, কোনো loading দেখাচ্ছে না"-র সরাসরি কারণ | `SomadhanViewModel.kt` (৪টা call-site: লাইন ~২৮৬১ `completeLoginAfterOtp`, ~৩০৭৬ `loginAsAdmin`, ~৩৩৪৭ `switchRoleToSolver`, ~৩৩৭২ `switchRoleToUser`), `LoginScreen.kt` (OTP confirm button onClick, লাইন ~৪৩৩-৪৬৫) | কোনো automated test নেই (UI timing/real-device-নির্ভর, ১.২/১.৪-এর মতোই) — ফিক্স: (ক) ৪টা call-site-এই `startRealtimeListeners()` কে `viewModelScope.launch { ... }` দিয়ে fire-and-forget করা হয়েছে, যাতে `onSuccess()/onReady()` আর তার জন্য অপেক্ষা না করে; (খ) `LoginScreen.kt`-এ `isVerifyingLoginOtp`/`showLoginOtpDialog` false করাটা `verifyOtp()`-এর onSuccess থেকে সরিয়ে `completeLoginAfterOtp()`-এর onSuccess-এ নেওয়া হয়েছে, যাতে dialog-টা "যাচাই হচ্ছে..." spinner-সহ পুরো সময় খোলা থাকে, ফাঁকা gap না থাকে — real-device manual verify বাকি (OTP login উভয় role-থাকা/single-role, admin login, profile role-switch — সবগুলোতেই এখন দ্রুত + visible loading আশা করা হচ্ছে) | ✅ |

**উপসর্গ যা এই একই বাগ কভার করে (ব্যবহারকারীর confirm করা উদাহরণ):**
- Bid accept করার পর balance কমে/escrow lock হয়, কিন্তু account-switch (logout/login) করার পর balance পুরনো মানে "reset" হয়ে যায়, escrow তখনও locked দেখায়।
- Escrow release করলেও balance আপডেট হয় না এই device-এ; SOLVER-এর transaction log-এ commission-বাদ amount ঠিকই লেখা কিন্তু balance ০।
- Withdraw request করার পর balance কমে, কিন্তু account-switch-এর পর balance আবার আগের (বড়) মানে ফিরে আসে — অথচ admin-এর দিকে withdrawal approved/pending-ই থেকে যায়।

**প্রস্তাবিত ফিক্স-দিক (Step 18.4-এ diff-আকারে দেওয়া আছে, প্রয়োগ হয়নি):** উপরের ৫টা ফাংশনের
প্রতিটাতে (role/account পরিবর্তনের ঠিক পরে) `SupabaseRealtimeManager.startRealtimeListeners()` আবার
কল করা (আর `logout()`-এ `stopRealtimeListeners()`)। `startUserTopicBroadcastSubscription()` নিজেই
idempotent বলে re-call নিরাপদ (নিজের doc-কমেন্টেই বলা আছে)।

---

## গ্রুপ ২ — 🔴 Admin panel — realtime/action-button বাগ (Step 19, ৪টা distinct বাগ)

| # | বাগ | ফাইল/ফাংশন | প্রমাণ (test) | Status |
|---|---|---|---|---|
| ২.১ (ক্লাস A) | `adminDashboardMetrics`-এ প্রতিটা headline metric-এ `if (supa.totalUsers > 0) supa.X else local.X` — একবার cloud RPC সফল হলে চিরকাল সেই stale snapshot আটকে থাকে, কখনো local reactive data-তে ফেরে না | `SomadhanViewModel.kt` লাইন ~১৯২০-২০১২ | `AdminPanelStep19RegressionTest.kt::testOverviewMetricsPermanentOverrideBugFixed` — কোড-ফিক্স + **Windows real-run দিয়ে pass কনফার্মড** (`.\gradlew.bat testDebugUnitTest --tests "*AdminPanelStep19RegressionTest*"` → এই টেস্টটা ৪টা ব্যর্থ টেস্টের তালিকায় নেই, বাকি ৪টা ব্যর্থতা Step 3-5-এর এখনো-অফিক্সড বাগের bug-demonstrating test, প্রত্যাশিতভাবেই fail, ব্যবহারকারীর মেশিনে, ২০২৬-০৯-২৩) | ✅ |
| ২.২ (ক্লাস B১) | `updateWithdrawalStatus()`-এ client-side status-guard silent no-op (PENDING না হলে চুপচাপ return), কিন্তু caller (`adminUpdateWithdrawalStatus()`, bulk-approve dialog) সবসময় "সফল" toast দেখায় | `SomadhanRepository.updateWithdrawalStatus()`, `SomadhanViewModel.kt` লাইন ৫৭৩০ এর কাছাকাছি (Somadhan Bug-Fix Step 3-এ shift হয়েছে), `AdminWithdrawalsView.kt` bulk-approve dialog | `AdminPanelStep19RegressionTest.kt::testWithdrawalStatusUpdateSurfacesFailureBugFixed` (Step 3-এ রিনেম, আগে `...SwallowsFailureBugStillPresent`) — **Windows real-run pass কনফার্মড (২০২৬-০৯-২৩)**, `8 tests completed, 3 failed`-এর ব্যর্থ-তালিকায় নেই | ✅ |
| ২.৩ (ক্লাস B২) | `adminRepairMissingRefunds()`/`adminReconcileBalances()`-এর `catch` ব্লকে `onComplete()` কল নেই → এরর হলে spinner চিরকাল আটকে থাকে (`AdminEscrowView.kt`-এ isRepairRunning/isReconcileRunning) | `SomadhanViewModel.kt` লাইন ~৬৫০২-৬৬০৬ (Somadhan Bug-Fix Step 4) | `AdminPanelStep19RegressionTest.kt::testReconcileAndRepairStuckSpinnerBugStillPresent` — কোড-ফিক্স + **Windows real-run দিয়ে pass কনফার্মড** (`.\gradlew.bat testDebugUnitTest --tests "*AdminPanelStep19RegressionTest*"` → `8 tests completed, 2 failed`-এর ব্যর্থ-তালিকায় এই টেস্ট নেই, ব্যবহারকারীর মেশিনে, ২০২৬-০৯-২৩) | ✅ |
| ২.৪ (ক্লাস B৩) | `AdminWithdrawalsView.kt`-এর ডিফল্ট (unfiltered) তালিকা non-reactive এক-বারের snapshot — কোনো mutation বা `refreshAdminTab()` pull-to-refresh-এ রিফ্রেশ হয় না, শুধু filter-toggle/re-entry-তে হয়; `AdminUsersView.kt`-এও একই architecture নিশ্চিত হয়েছে (Somadhan Bug-Fix Step 5-এ verify করা হয়েছে — `isBrowsingUnfiltered`+`viewModel.adminUsersPaged` প্যাটার্ন সত্যিই আছে) | `AdminWithdrawalsView.kt` লাইন ৩৮৯-৪২৭, `SomadhanViewModel.kt`-এ `adminUpdateWithdrawalStatus()` (~৫৭৪৫), `refreshAdminTab()` (~২৫১৯), `adminWithdrawalsPaged`/`loadNextAdminWithdrawalsPage()`/`resetAdminWithdrawalsPagination()` (~১০৩০-১০৬০) | `AdminPanelStep19RegressionTest.kt::testAdminWithdrawalsPagedStalenessBugStillPresent` + `testRefreshAdminTabDoesNotResetWithdrawalsOrUsersPagedCacheBugStillPresent` — কোড-ফিক্স + **Windows real-run দিয়ে pass কনফার্মড** (`.\gradlew.bat testDebugUnitTest --tests "*AdminPanelStep19RegressionTest*"` → `BUILD SUCCESSFUL`, ব্যবহারকারীর মেশিনে, ২০২৬-০৯-২৩) | ✅ |

**প্রস্তাবিত ফিক্স (Step 19.6-এ diff-আকারে দেওয়া আছে):**
- ২.১: ✅ **প্রয়োগ হয়েছে (Somadhan Bug-Fix Step 2)** — permanent override বাদ দিয়ে সবসময় local
  reactive aggregate (`localUsers.size` ইত্যাদি) ব্যবহার।
- ২.২: status-guard fail হলে caller-কে জানানো (exception/Result টাইপ দিয়ে), toast conditional করা।
- ২.৩: ✅ **প্রয়োগ হয়েছে + Windows-verified (Somadhan Bug-Fix Step 4)** — `catch` ব্লকে zero/no-op
  রিপোর্ট দিয়ে `onComplete()` কল যোগ করা হয়েছে (finally-এর মতো আচরণ)।
- ২.৪: ✅ **প্রয়োগ হয়েছে + Windows-verified (Somadhan Bug-Fix Step 5)** — `adminUpdateWithdrawalStatus()`-এর `Updated` branch-এ `resetAdminWithdrawalsPagination()` কল করা হয়েছে; `refreshAdminTab()`-এ `resetAdminWithdrawalsPagination()` ও `resetAdminUsersPagination(currentAdminUsersRoleFilter)` (বর্তমান role-filter সংরক্ষণ করে, silent "ALL"-রিসেট এড়াতে) দুটোই unconditionally যোগ করা হয়েছে। `AdminUsersView.kt`-এর একই architecture যাচাই করে নিশ্চিত হয়েছে (`isBrowsingUnfiltered`+`viewModel.adminUsersPaged` — সত্যিই মিলেছে), কিন্তু সেই স্ক্রিনের নিজস্ব mutation ফাংশন (ban/restrict/delete ইত্যাদি) touch করা হয়নি কারণ কোনো test/bug-row সেটার জন্য নির্দিষ্ট দাবি করেনি (rule #৩, স্কোপ-ক্রিপ এড়ানো) — `refreshAdminTab()`-এর মাধ্যমে pull-to-refresh-এই এটা কভার্ড।

---

## গ্রুপ ৩ — ✅ Offline-action-gating toggle বাগ (Step 16, কোনো automated test নেই, real-device verified)

| # | বাগ | ফাইল/ফাংশন | প্রমাণ (test) | Status |
|---|---|---|---|---|
| ৩.১ | রিপোর্ট: "toggle disable করলেও offline-এ app-এ ঢোকা যাচ্ছিল" — ৩টা স্বাধীন সম্ভাব্য কারণ পাওয়া গেছে, নিশ্চিতভাবে কোনটা (বা কম্বিনেশন) দায়ী তা এখনো টেস্ট দিয়ে প্রমাণ হয়নি | নিচে ৩টা উপ-আইটেম | ৩.১b Windows-verified ✅। ৩.১a/৩.১c কোড-ফিক্স হয়েছে কিন্তু কোনো automated test নেই (runtime/RLS/UI-নির্ভর) — real-device-এ ব্যবহারকারী কনফার্ম করেছেন — ৩.১a/৩.১b/৩.১c তিনটাই perfectly কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ৩.১a | `strict_offline_block` platform-setting-এর `updatePlatformSetting()` dual-write সাইট Step 12-এর outbox-retry protection-এর বাইরে (money-related না বলে তখন স্কোপে ছিল না) — cloud-write ব্যর্থ হলে caller জানতে পারে না | `SomadhanRepository.updatePlatformSetting()`, caller `SomadhanViewModel.adminUpdatePlatformSetting()` | কোনো automated test নেই (runtime Supabase-নির্ভর) — sealed `PlatformSettingUpdateResult` (Updated/CloudSyncFailed) দিয়ে কোড-ফিক্স হয়েছে (Somadhan Bug-Fix Step 6, sub-step ৩)। Windows build-এ compile-fail ধরা পড়েছিল (`SomadhanViewModel.kt`-এ `PlatformSettingUpdateResult`-এর import মিসিং ছিল) — এই সেশনে import যোগ করে ফিক্স করা হয়েছে (২০২৬-০৯-২৩), কিন্তু এখনো real build দিয়ে re-verify হয়নি — real-device-এ ব্যবহারকারী কনফার্ম করেছেন perfectly কাজ করছে (২০২৬-০৯-২৪) | ✅ |
| ৩.১b | `requireOnlineOrWarn()`-এর `_isOnline` শুধু `NET_CAPABILITY_INTERNET` চেক করত, `NET_CAPABILITY_VALIDATED` না — captive-portal/fake-online false-positive সম্ভব ছিল | `SomadhanViewModel.kt` `startConnectivityObserver()` (~১৮৩০-১৯০২) | `OfflineGatingSyncTest.kt::` `` `startConnectivityObserver checks NET_CAPABILITY_VALIDATED before marking isOnline true (BUG 3_1b fixed)` `` — কোড-ফিক্স + **Windows real-run দিয়ে pass কনফার্মড** (`.\gradlew.bat testDebugUnitTest --tests "*OfflineGatingSyncTest*"` → `BUILD SUCCESSFUL`, ব্যবহারকারীর মেশিনে, ২০২৬-০৯-২৩) | ✅ |
| ৩.১c | `loginAsAdmin()`-এ real Supabase Auth সাইন-ইন ব্যর্থ হলেও degraded local-only সেশনে এগিয়ে যাওয়া হয়, যেখানে `is_admin(auth.uid())` RLS-গেটেড সব cloud write নীরবে ব্যর্থ হয় — আগে শুধু transient (২-৩ সেকেন্ড) toast দেখাত | `SomadhanViewModel.loginAsAdmin()` (নতুন `isDegradedAdminSession`/`degradedAdminSessionReason` StateFlow + `dismissDegradedAdminSessionWarning()`), `AdminPanelScreen.kt` (নতুন `DegradedAdminSessionBar` composable, topBar-এ RealtimeLocationBar-এর নিচে) | কোনো automated test নেই (Compose UI/runtime Supabase-নির্ভর) — persistent, dismissible bar কোড-ফিক্স হয়েছে (Somadhan Bug-Fix Step 6, sub-step ৪, ব্যবহারকারীর পছন্দমতো "(ক) persistent bar" approach), শুধু কোড-লেভেল sanity-check (brace-balance) — real-device-এ ব্যবহারকারী কনফার্ম করেছেন perfectly কাজ করছে (২০২৬-০৯-২৪) | ✅ |

**প্রস্তাবিত ফিক্স-দিক (Step 16.2-এ দেওয়া, প্রয়োগ হয়নি):** (১) dual-write ব্যর্থতা caller-কে জানানো;
(২) `_isOnline`-এ `NET_CAPABILITY_VALIDATED` যোগ করা; (৩) degraded-সেশন সতর্কতা persistent করা বা
Settings-এ session-status দেখানো।

---

## গ্রুপ ৪ — 🟡 UI transaction sign/color display বাগ (Step 15 — কোড ফিক্স হয়ে গেছে)

| # | বাগ | ফাইল/ফাংশন | প্রমাণ (test) | Status |
|---|---|---|---|---|
| ৪.১ | `transactions.type == ADMIN_ADJUSTMENT` (এবং `WITHDRAWAL_REFUND`) নীরবে সবসময় "−" (deduct) দেখাত, actual add/deduct নির্বিশেষে — কারণ প্রতিটা স্ক্রিনে independent enumeration-ভিত্তিক sign-লজিক ছিল যেটা এই টাইপগুলো মিস করত | `TransactionHistoryScreen.kt`, `UserWalletScreen.kt`, `DashboardScreen.kt` | `TransactionDisplaySignTest.kt` (২৪ কেস) + `WalletDashboardSignWiringTest.kt` (৪ কেস) — **Step 20.5-এর পূর্ণাঙ্গ Windows-run-এ ২৮/২৮ pass কনফার্মড** | ✅ |

**ফিক্স ইতিমধ্যেই প্রয়োগ হয়ে গেছে:** নতুন pure function `transactionDisplaySign(trx, viewerId)` —
single source of truth `net_amount >= 0.0` (DB-তেই sign-সহ লেখা হয় insert-টাইমে) — তিনটা স্ক্রিনেই
ব্যবহৃত। **এই একটা গ্রুপের জন্য fix master prompt-এ আলাদা কোনো Step লাগবে না**, শুধু re-confirm।

---

## গ্রুপ ৫ — ✅ Money-critical dual-write/RPC গ্যাপ (Step 12 সিরিজ — সব ফিক্স হয়ে গেছে, Windows-verified)

Step 12.1–12.12 জুড়ে পাওয়া প্রায় ৩০+টা dual-write/atomicity সাইট (bid accept, job release, dispute,
admin reconcile/refund/cleanup, wallet/withdrawal/refund/additional-charge, extra-payment/direct-contract/
commission, borderline cases) — **সবগুলোই কোড-ফিক্স হয়েছে এবং Windows real-run দিয়ে pass কনফার্মড।**
সাথে SQL RPC guard-ও applied+verified: `release_escrow` dispute guard (12.10), `refund_escrow_once`
hardening (12.10c), `request_withdrawal` SOLVER KYC guard (12.10e), client-supplied escrow-id (12.11)।
এই গ্রুপের জন্য fix master prompt-এ কোনো Step লাগবে না — শুধু reference হিসেবে রাখা হলো যাতে ভুলে আবার
touch না করা হয়।

---

## গ্রুপ ৬ — ✅ CI-infra বাগ (Step 13 — ফিক্স হয়ে গেছে, user-facing না)

`realtime_scoping_step3_messages_broadcast.sql`-এর migration-apply-order বাগ (alphabetical vs
chronological, policy already-exists এরর) — ১-লাইন `drop policy if exists` গার্ড যোগ করে ফিক্স, real
psql+pgTAP fresh-DB run-এ ১১৩২/১১৩২ assertion pass কনফার্মড। **এটা production app-এর বাগ না** (শুধু CI
sandbox-এর migration-apply-order সমস্যা), user-facing কোনো প্রভাব নেই — reference-এর জন্য রাখা হলো।

---

## গ্রুপ ৭ — 📝 Backlog / decision-needed (Step 20 audit, money-critical না, urgent না)

| # | আইটেম | বিস্তারিত | Status |
|---|---|---|---|
| ৭.১ | ~~`submit_rating` RPC-এ one-time-per-problem idempotency guard নেই~~ **[ফিক্সড, ২০২৬-০৯-২৪]** | retry করলে ডুপ্লিকেট rating row তৈরি হতে পারত (money-table না, কিন্তু data-integrity ইস্যু)। ফিক্স: `submit_rating()`-এ insert-এর আগে (problem_id, rater_role) দিয়ে existing rating চেক করা হচ্ছে — পাওয়া গেলে নতুন row না বানিয়ে existing rating_id-ই `already_rated: true` ফ্ল্যাগসহ ফেরত দেওয়া হয় (idempotent)। নতুন migration: `zz_20260924120000_step7_1_submit_rating_idempotency_guard.sql`। `supabase/tests/11_notifications_ratings_part2.sql`-এর পুরনো "DOCUMENTED CURRENT BEHAVIOUR — duplicate গার্ড নেই" টেস্ট নতুন idempotent behavior-এর সাথে মিলিয়ে আপডেট করা হয়েছে (plan ১৬০→১৬২), Kotlin caller (`SupabaseSyncManager.submitRating()`) অপরিবর্তিত (raw JSON রিটার্ন করে, নতুন ফিল্ড ভাঙে না)। **Migration Supabase MCP দিয়ে সরাসরি live project (`somadhan`, `mghvvpndkxnscwryfkib`)-এ apply করা হয়েছে ও `pg_get_functiondef()` দিয়ে cloud-এর function definition নতুন কোডের সাথে হুবহু মিলে যাচ্ছে কনফার্ম করা হয়েছে (২০২৬-০৯-২৪)** — pgTAP suite (part2) app-এর নিজস্ব CI-তে চালিয়ে ১৬২/১৬২ pass চূড়ান্ত কনফার্ম বাকি | ✅ |
| ৭.২ | `admin_force_cancel_instant_job` নিজে escrow refund/release করে না | RPC-ডিজাইনের প্রশ্ন — ইচ্ছাকৃত নাকি গ্যাপ, user-এর সিদ্ধান্ত লাগবে। **তদন্ত-নোট (২০২৬-০৯-২৪, কোনো কোড বদলানো হয়নি):** এই RPC-র একমাত্র caller `SomadhanRepository.adminForceCancelInstantJob()` (লাইন ~১১২৭৪); সেটা RPC কলের **আগেই** লোকাল-এ `refundEscrowOnce()` দিয়ে (নিজস্ব dual-write/outbox সহ, Step 12) escrow refund করে ফেলে, আর কোডের কমেন্টেই স্পষ্ট লেখা "escrow refund এখানে ডুপ্লিকেট করা হয়নি" — অর্থাৎ ডিজাইনটা ইচ্ছাকৃত layering বলেই মনে হচ্ছে (RPC শুধু problem/bids state + notification, refund আলাদা RPC-তে)। বাকি ঝুঁকি: refund-এর cloud-write ব্যর্থ হলে সেটা Step 12-এর outbox-retry-ই ধরে, এই RPC-র দায় না। **সিদ্ধান্ত: (ক) এখনই ফিক্স (ব্যবহারকারী, ২০২৬-০৯-২৪)।** ফিক্স: নতুন migration `zz_20260924130000_step7_2_force_cancel_instant_job_refunds_held_escrow.sql` — (১) accepted solver থাকলে (`v_had_solver`) সেই problem-এর প্রতিটা HELD escrow-এ `refund_escrow_once(id,'ADMIN_FORCE_ACTION',100)` কল (তিনটা target_action-এই); `refund_escrow_once` idempotent বলে app আগে refund করলে double-refund নেই। (২) owner-এর USER role নিষ্ক্রিয় (`USER_ROLE_INACTIVE`) হলে (ব্যবহারকারীর সিদ্ধান্ত) cancel থামে না — cancel সম্পন্ন, escrow HELD, RPC ফলাফলে `refund_pending: true`; অন্য যেকোনো refund error-এ পুরো rollback। (৩) নতুন trigger `trg_refund_pending_escrows_on_user_role_activation` (`users.has_user_role` false→true): সেই user-এর HELD escrow যার problem CANCELLED বা (OPEN + accepted_solver_id null) সেগুলো `refund_escrow_once(...,'ROLE_REACTIVATION_REFUND',100)` দিয়ে স্বয়ংক্রিয় refund; trigger কখনো role-activation আটকায় না (ব্যর্থ হলে WARNING, escrow HELD-ই)। pgTAP `10_admin_moderation_balance_part2.sql`: পুরনো "HELD-ই থাকে" assertion উল্টে REFUNDED, REFUND transaction, no-double-refund (E_FC2), নিষ্ক্রিয়-role scenario (FC6/E_FC6: cancel OK + refund_pending, escrow HELD; role active করার পর trigger-refund) — plan ১৯৬→২০১। Kotlin অপরিবর্তিত। **Live Supabase-এ Supabase MCP দিয়ে apply করা হয়েছে (২০২৬-০৯-২৪)** — আগে cloud-এ পুরনো বডি (refund-হীন) ছিল ও `escrows`-এ HELD-orphan ০টা; apply-এর পর `admin_force_cancel_instant_job`-এ নতুন বডি (`v_refund_pending`), `refund_pending_escrows_on_user_role_activation()` ফাংশন ও `trg_refund_pending_escrows_on_user_role_activation` trigger (enabled, WHEN শর্তসহ) কনফার্মড, grants অপরিবর্তিত। কোনো আচরণগত (behavioural) টেস্ট চালানো হয়নি। **বাকি: pgTAP real-run (১০_…part2 = ২০১) / real-device দিয়ে নিষ্ক্রিয়-role scenario verify** | 🟡 |
| ৭.৩ | `syncAllLocalToSupabase`-এর bootstrap-sync (`createProblem`/`createBid`)-এর নিজস্ব self-designed idempotent retry-loop, স্ট্যান্ডার্ড `enqueueOutboxRetry` প্যাটার্নের সাথে সাংঘর্ষিক হতে পারে | ভবিষ্যতে কোনো নতুন retry-mechanism যোগ করার আগে review দরকার **[২০২৬-০৯-২৪ — ব্যবহারকারীর সিদ্ধান্ত (গ): closed, intentional]** — কোড পড়ে যাচাই: ফাংশন শুধু লগইন-করা ইউজারের নিজের profile/problem/bid best-effort push করে, insert idempotent (duplicate-key cloud নিজেই আটকায়), কোনো টাকা-টেবিল নেই, outbox ব্যবহার করে না। কোনো কোড বদলানো হয়নি। শুধু ভবিষ্যতে নতুন retry-mechanism যোগ করলে এর সাথে ওভারল্যাপ ভাবতে হবে। | ✅ closed |
| ৭.৪ | `ExampleRobolectricTest`-এ ৩টা flaky/intermittent fail (১৬টার মধ্যে) | Robolectric-sandbox-init-স্তরের পরিচিত সমস্যা, কোনো session-ই কখনো ছোঁয়নি, app বাগ না **[২০২৬-০৯-২৪ — ব্যবহারকারীর সিদ্ধান্ত (গ): closed, intentional]** — Step 8-এর full-suite run-এ এই ক্লাসের flaky fail বাদ রেখেই green ধরা হবে। কোনো কোড বদলানো হয়নি। | ✅ closed |
| ৭.৫ | Step 14.1-এ পাওয়া, স্কোপের বাইরে রাখা: `bids_select`/`problems_select` (table-level RLS) কোনো migration-এ CREATE হয়নি (শুধু ALTER, লাইভে সরাসরি তৈরি) — এই দুটো policy-র test-coverage নেই | নতুন inferred schema-stub লাগবে টেস্টের জন্য — এটা coverage-gap, confirmed বাগ না **[২০২৬-০৯-২৪ — ব্যবহারকারীর সিদ্ধান্ত (খ): পরে, খোলা রাখা হলো]** — এখন কোনো কাজ নেই; ভবিষ্যতে RLS policy বদলানোর আগে/সময় live থেকে policy definition পড়ে schema-stub + pgTAP যোগ করা হবে। | 📝 deferred |
| ৭.৬ | Somadhan Bug-Fix Step 6 টেস্টের সময় ধরা পড়েছে (১.২-এর একই root-cause প্যাটার্ন): `SupabaseRealtimeManager.kt`-এর আরও ৩টা `.subscribe()` কলে (`joinProblemMessagesBroadcastChannel()`, `joinProblemBidsBroadcastChannel()`, আরেকটা চ্যাট/ডিসপিউট-সংশ্লিষ্ট per-problem channel) একই timeout-না-থাকার সমস্যা আছে — ইচ্ছাকৃতভাবে ছোঁয়া হয়নি (লগইন/রোল-সুইচ path-এর অংশ না) | Chat/problem-detail স্ক্রিন অফলাইনে বা network-issue-তে খোলার চেষ্টা করলে একইভাবে আটকে যাওয়ার সম্ভাবনা আছে — ১.২-এর মতোই `subscribeWithTimeout()` হেল্পার দিয়ে ফিক্স করা যাবে, কিন্তু আলাদা confirm/reproduce দরকার **[২০২৬-০৯-২৪ — ব্যবহারকারীর সিদ্ধান্ত (ক): ফিক্স করা হলো]** — `SupabaseRealtimeManager.kt`-এর তিনটা bare `ch.subscribe()` (`joinProblemMessagesBroadcastChannel`, `joinProblemBidsBroadcastChannel`, `joinTypingChannel`) → `ch.subscribeWithTimeout(label)` (১.২-এর হেল্পার, ১০ সেকেন্ড, non-fatal; কোনো নতুন হেল্পার/সিগনেচার বদল নেই)। timeout হলে channel map-এ cached থাকে (live push ছাড়া), কিন্তু স্ক্রিন বেরোলে `leave…()` map থেকে সরায়, তাই পরের open-এ আবার subscribe চেষ্টা হয়; কাজ চালিয়ে যায় Room/bulk-pull/pull-to-refresh দিয়ে। কোনো automated test নেই (১.২/১.৪-এর মতোই network-timing-নির্ভর); sanity: brace-balance অপরিবর্তিত (৩৭৬/৩৭৬), diff শুধু ৩টা লাইন। **বাকি: real-device verify (chat/problem-detail স্ক্রিন স্বাভাবিক খোলে + খারাপ network-এও আটকে থাকে না) ও Windows build** | 🟡 |
| ৭.৭ | ২০২৬-০৯-২৪ পূর্ণ অডিট: `SomadhanRepository.kt`-এ `pendingCloudSync = true` সেট করা সব ১৪টা call-site (`payoutEscrowToSolver`, `acceptBid`, `adminResolveDisputeLocked`, `refundEscrowOnceLocked`, `reconcileUserBalances`, `cleanupDuplicateRefunds`, `repairMissingRefunds`, `confirmReleaseAndComplete`, `requestWithdrawal`, `updateWithdrawalStatus`, `depositMoneyViaGateway`, `adminAdjustBalance`, `respondToAdditionalCharge`, `userConfirmExtraAmount`) grep+read করে যাচাই করা হয়েছে — **প্রতিটাতেই** আসল cloud-sync হয় একটা Supabase RPC dual-write দিয়ে, ব্যর্থ হলে `enqueueOutboxRetry()`/outbox queue দিয়ে retry হয় — কোথাও কোনো real retry-gap নেই, শুধু কমেন্ট ভুল নাম (`syncPendingCloudRefunds()`) বলছিল | **কোনো functional বাগ ছিল না** — pure documentation/comment issue। ফিক্স: সব ১৪টা call-site + ২টা helper comment (`refundEscrowOnceLocked` boilerplate, `reconcileUserBalances` doc-comment) + `OutboxSyncWorker.kt`-এর ১টা রেফারেন্স মিলিয়ে ~১৯টা জায়গার কমেন্ট সঠিক করা হয়েছে (আসল মেকানিজম: RPC dual-write + outbox retry); `MarketEntities.kt`-এ `pendingCloudSync`/`cloudBalanceSynced` field declaration-এর উপরে একটা স্থায়ী নোট যোগ করা হয়েছে যাতে ভবিষ্যতে কেউ এগুলোকে সক্রিয় মেকানিজম না ভাবে। কোনো functional/behavioral কোড বদলানো হয়নি, তাই কোনো নতুন test/Windows-run দরকার নেই | ✅ |
| ৭.৮ | ৭.২-এর সময় ধরা পড়েছে: `admin_reconcile_escrow_states()`-এর Case 2/3 লুপে `perform refund_escrow_once(...)` কোনো exception-হ্যান্ডলিং ছাড়া — একটা escrow-এর owner-এর USER role নিষ্ক্রিয় (`USER_ROLE_INACTIVE`) হলে পুরো reconcile run rollback হয়ে বাকি সব escrow-ও repair হয় না | ৭.২-এর ফিক্সের পর এমন pending-refund escrow বাড়তে পারে (role active না হওয়া পর্যন্ত HELD), তাই admin reconcile বারবার আটকে যাওয়ার সম্ভাবনা বাড়ে। **সিদ্ধান্ত: (ক) এখনই ফিক্স (ব্যবহারকারী, ২০২৬-০৯-২৪)।** ফিক্স: নতুন migration `zz_20260924140000_step7_8_admin_reconcile_escrow_states_per_escrow_exception_handling.sql` — Case 2 (`refund_escrow_once`) ও Case 3 (`release_escrow`)-এর প্রতিটা `perform` কল `admin_force_cancel_instant_job` (Step 7.2)-এর precedent অনুযায়ী নেস্টেড `begin...exception when others...end;` ব্লকে মোড়ানো হয়েছে (plpgsql নেস্টেড ব্লক = implicit savepoint, তাই একটা escrow ব্যর্থ হলে শুধু সেটাই বাদ পড়ে, বাকি লুপ rollback হয় না)। ব্যর্থ escrow `v_items`-এ `'status':'failed'` + `sqlerrm`-সহ রিপোর্ট হয়; নতুন `case2_failed`/`case3_failed` কাউন্টার রিটার্ন-জেসনে যোগ হয়েছে (audit-log `details`-এর বিদ্যমান exact-string format অপরিবর্তিত রাখা হয়েছে, পুরনো pgTAP assertion না ভাঙার জন্য)। `case2_repaired`/`case3_repaired` এখন শুধু আসল সফল repair গোনে। pgTAP `10_admin_moderation_balance_part2.sql`-এ নতুন ৩য়-লাইভ-রান scenario (E_RC10..E_RC13: ২টা সুস্থ + owner/solver role-নিষ্ক্রিয় ২টা) দিয়ে ৭টা নতুন assertion (plan ২০১→২০৮) — যাচাই করে সফল দুটো ঠিকভাবে repair হয়েছে, ব্যর্থ দুটো HELD-ই থেকেছে (rollback হয়নি), items-এ failed+error রিপোর্ট, ও audit log ঠিকমতো লেখা হয়েছে। Kotlin অপরিবর্তিত (এই RPC-র caller `SomadhanRepository`-তে শুধু রেজাল্ট log/toast করে, নতুন ফিল্ড ভাঙে না)। **Live Supabase-এ Supabase MCP দিয়ে apply করা হয়েছে (২০২৬-০৯-২৪)** — apply-এর আগে/পরে `pg_get_functiondef()` দিয়ে cloud-এর function-বডি যাচাই করা হয়েছে (আগে পুরনো non-exception-handled বডি ছিল, এখন নতুন বডি হুবহু মিলছে), grants অপরিবর্তিত (postgres/authenticated/service_role)। **বাকি: pgTAP real-run (১০_…part2 = ২০৮) sandbox-এ network বন্ধ থাকায় করা যায়নি, static-ভাবেই ফাংশন-বডি ও `refund_escrow_once`/`release_escrow`-এর real exception-message মিলিয়ে যাচাই করা হয়েছে** | 🟡 |
| ৭.৯ | ৭.২-এর সময় ধরা পড়েছে: `admin_force_cancel_instant_job` এখন `refund_pending: true` ফেরত দিতে পারে, কিন্তু `SupabaseSyncManager.adminForceCancelInstantJob()`/Kotlin/admin UI এটা পড়ে না — admin-কে জানানো হয় না যে refund পেন্ডিং | ছোট UI/toast কাজ। **সিদ্ধান্ত: (ক) এখনই ফিক্স করুন (ব্যবহারকারী, ২০২৬-০৯-২৪)।** ফিক্স: `SomadhanRepository.adminForceCancelInstantJob()` রিটার্ন-টাইপ `Unit`→`Boolean` (`refundPending`) — RPC জবাবের `refund_pending` ফ্ল্যাগ (`acceptBid()`-এর 'result' field পড়ার একই fully-qualified `kotlinx.serialization.json.JsonObject`/`JsonPrimitive` প্যাটার্নে, `.content?.toBooleanStrictOrNull()`) পার্স করে ফেরত দেয় (early-return/offline/dual-write-ব্যর্থ path-এ false)। `SomadhanViewModel.adminForceCancelInstantJob()` এই ফ্ল্যাগ পড়ে refundPending হলে আলাদা toast দেখায় ("...ইউজারের অ্যাকাউন্ট নিষ্ক্রিয় থাকায় রিফান্ড আটকে আছে — অ্যাকাউন্ট সক্রিয় হলে স্বয়ংক্রিয়ভাবে রিফান্ড হবে"), নাহলে আগের toast-ই। কোনো UI/SQL/migration বদলায়নি, শুধু ২টা Kotlin ফাইল। কোনো automated test নেই (RPC response-নির্ভর, real-network); sanity: brace-balance দুই ফাইলেই আগে-পরে হুবহু সমান। **বাকি: real-device/manual verify (একজন role-নিষ্ক্রিয় owner-এর accepted-solver instant job admin force-cancel করে নতুন toast দেখা যাচ্ছে কিনা)** | 🟡 |

---

## সারসংক্ষেপ — কী fix করতে হবে (fix master prompt-এর scope)

| গ্রুপ | বাগ-সংখ্যা | Status | Fix master prompt-এ Step? |
|---|---|---|---|
| ১ — Realtime subscription (account-switch) | ১ | ✅ (fixed + Windows-verified) | **হ্যাঁ — Step 1 (সম্পূর্ণ)** |
| ২ — Admin panel | ৪ | ✅✅✅✅ (২.১–২.৪ সবই fixed+verified) | **হ্যাঁ — Step 2–5 সম্পূর্ণ+verified, পুরো গ্রুপ ২ শেষ** |
| ৩ — Offline-gating toggle | ১ (৩ উপ-কারণ) | 🟠 | **হ্যাঁ — Step 6** |
| ৪ — UI transaction sign | ১ | ✅ (fixed) | Step 0-এ শুধু re-confirm |
| ৫ — Money dual-write (Step 12) | ৩০+ | ✅ (fixed+verified) | না, touch করার দরকার নেই |
| ৬ — CI migration-order | ১ | ✅ (fixed) | না |
| ৭ — Backlog | ৫ | 📝 | **হ্যাঁ — Step 7 (আলোচনা, fix না)** |

**মোট নতুন কোড-ফিক্স লাগবে এমন বাগ: ৬টা (গ্রুপ ১, ২-এর ৪টা, ৩)।**
