# সমাধান অ্যাপ — শিমার/লোডিং রিগ্রেশন বাগ ফিক্স — ব্যাচ ৩১+ মাস্টার প্রম্পট

> **এই ফাইল কেন:** `SOMADHAN_LOADING_PATTERN_MASTER_PROMPT.md` (ব্যাচ ৩০) অনুযায়ী প্রজেক্টের সব
> স্ক্রিনে rule ১-৪ (cold load / re-entry / pull-to-refresh / pagination) কাঠামোগতভাবে বসানো শেষ
> হয়েছিল, কিন্তু কখনো real Android Studio build/run-এ যাচাই হয়নি (Ground Rule ২ তখনও অপূর্ণ ছিল)।
> ব্যবহারকারী এখন real device/emulator-এ অ্যাপ চালিয়ে যে আসল বাগগুলো পেয়েছেন সেগুলোর একটা পূর্ণ
> তালিকা — এই ফাইলে সেগুলোকেই ছোট ছোট, independently-shippable সেশনে ভাগ করা হলো।
>
> **আবশ্যিক পূর্বশর্ত:** এই কাজ শুরুর আগে অবশ্যই `SOMADHAN_LOADING_PATTERN_MASTER_PROMPT.md` পুরোটা
> পড়ে নিতে হবে — চারটা কোর কম্পোনেন্ট (`rememberSessionAwareSkeletonGate`, `SyncAwareContent`,
> `rememberFieldChangePulse`, pagination প্যাটার্ন) এবং Ground Rules ১-১৩ এখানে পুনরায় লেখা হয়নি,
> ওগুলোই বহাল থাকবে। এই ফাইলে শুধু **নতুন পাওয়া বাগ** আর তার ফিক্স-পরিকল্পনা আছে।

---

## ধাপ ০ — এই কাজের জন্য তিনটা অতিরিক্ত Ground Rule (আগের ১৩টার সাথে যোগ হবে)

14. **প্রতিটা স্ক্রিন ফিক্স করার সময় দুটো systemic প্যাটার্ন সবসময় চেক করতে হবে** (নিচে ধাপ ১-এ
    বিস্তারিত), শুধু ওই স্ক্রিনের নাম-করা বাগটাই না:
    - (ক) **"শিমার আটকে থাকা" (endless/stuck shimmer)** — কোনো action-এর (recharge, submit,
      ইত্যাদি) পরে loading phase কখনো `LOADED`-এ ফিরছে না, অন্য পেজে গিয়ে আবার এলে ঠিক হয়ে যায়।
    - (খ) **"random auto full-page shimmer"** — কোনো user action ছাড়াই মাঝে মাঝে পুরো পেজ আবার
      পুরোপুরি শিমার হয়ে যাচ্ছে (রি-এন্ট্রি/pull-to-refresh না হয়েও)।
    এই দুইটা যেখানেই পাওয়া যাবে (এই ফাইলে তালিকাভুক্ত স্ক্রিন না হলেও) ঠিক করতে হবে, কিন্তু ধাপ ৪-এর
    নিয়ম অনুযায়ী ছোট commit-এ, আর প্রতিবার ঠিক কোন ফাইলে/কেন এই প্যাটার্ন পাওয়া গেছে তা progress
    note-এ লিখতে হবে।
15. **শিমার/স্কেলিটনের shape পেজ-নির্দিষ্ট হতে হবে** — একই জেনেরিক rectangle/box shimmer সব পেজে
    কপি-পেস্ট করা যাবে না। প্রতিটা section-এর real content layout (card/list-item/value-row/ইত্যাদি)
    যেমন দেখতে, স্কেলিটনও ঠিক তেমন কাঠামোতে (একই height/spacing/rounded-corner/আইকন-প্লেসহোল্ডার
    বিন্যাস) বানাতে হবে, যাতে skeleton → real content swap-এ কোনো layout-jump না হয়। একটা shared
    generic `GenericCardSkeleton()` থাকতে পারে reusable building block হিসেবে, কিন্তু প্রতিটা স্ক্রিন
    সেটাকে নিজের real layout অনুযায়ী compose করবে — কপি-পেস্ট generic box না।
16. **অস্পষ্টতায় অনুমান নয়, আগে প্রশ্ন করতে হবে** — কোনো স্ক্রিন/ফাইলের নাম, বর্তমান আচরণ, স্কোপ,
    কোনো ফিক্সের approach, বা কোনো সাজেশন/decision-এর দরকার হলে সামান্যতম অস্পষ্টতা বা সন্দেহ থাকলেও
    নিজে থেকে অনুমান করে কাজ চালিয়ে যাওয়া যাবে না। সেক্ষেত্রে থেমে ব্যবহারকারীকে স্পষ্ট প্রশ্ন করতে
    হবে, উত্তর/কনফার্মেশন পাওয়ার পরই কাজ এগোবে। **এই নিয়ম কোনো অবস্থাতেই লঙ্ঘন করা যাবে না** — সময়/
    কনটেক্সট বাঁচানোর জন্যও না।

---

## ধাপ ০.৫ — গুরুত্বপূর্ণ নোট: user-এর জন্য বলা ফিক্স solver-এর সমতুল্য পেজেও প্রযোজ্য

ধাপ ২-এ যেসব সেশনে "user" পেজের নাম করে ফিক্স চাওয়া হয়েছে, সেই একই ফিক্স/আচরণ সোলভার রোলের
সমতুল্য পেজের জন্যও কার্যকর হতে হবে। কোডবেস যাচাই করে (grep, ২০২৬-০৯-১৪ পর্যন্ত আপলোড করা zip
অনুযায়ী) নিচের দুই ধরনের কেস পাওয়া গেছে:

- **শেয়ার্ড স্ক্রিন (একই ফাইল, `isSolver`/`currentUser.role` চেক দিয়ে ভেতরে ব্রাঞ্চ করা)** —
  `DashboardScreen.kt`, `UserWalletScreen.kt`, `TransactionHistoryScreen.kt`, `ProfileScreen.kt`,
  `FaqScreen.kt`, `InstantJobsScreen.kt`/`InstantJobHistoryScreen.kt`। এগুলোতে ফিক্স একই ফাইলে হয়
  বলে code পরিবর্তন এমনিতেই দুই role-এর জন্যই প্রযোজ্য হয়ে যায় — কিন্তু **টেস্ট/ভেরিফিকেশন সবসময়
  `isSolver = true` এবং `isSolver = false` দুই ব্রাঞ্চেই আলাদাভাবে grep করে করতে হবে**, কারণ
  syncPhase/state variable কোনো role-branch-এ ভিন্নভাবে সেট হতে পারে (যেমন solver-branch-এ আলাদা
  ViewModel flow ব্যবহার হচ্ছে কিনা)। শুধু user-role code-path verify করে "ঠিক আছে" ধরে নেওয়া যাবে
  না।
- **আলাদা ফাইল (সত্যিকারের সোলভার-স্পেসিফিক স্ক্রিন)** — যেমন `UserWithdrawScreen.kt` vs
  `SolverBalanceWithdrawScreen.kt`, বা `UserProblemsScreen.kt`/`ProblemDetailScreen.kt` vs
  `SolverProblemsScreen.kt`/`SolverAllPostsScreen.kt`/`SolverMyBidsScreen.kt`। এগুলোতে একই
  ফিক্স-প্যাটার্ন সোলভার ফাইলে আলাদাভাবে প্রয়োগ করতে হবে (নিচে প্রতিটা সেশনে স্পষ্ট করে লেখা আছে)।
- **যেসব ফিচার সত্যিই শুধু user-সাইড, সোলভারে কোনো সমতুল্য নেই** (যেমন `FavoriteSolversScreen.kt`)
  — সেগুলোতে জোর করে সোলভার-ভার্সন বানানোর দরকার নেই, নিচে সেই সেশনে স্পষ্ট করে লেখা আছে কেন নেই।

কোনো সেশনে যদি কোনো স্ক্রিনের role-equivalent নিয়ে এই তালিকার বাইরে নতুন অস্পষ্টতা দেখা দেয়, ধাপ
০.১৬ অনুযায়ী অনুমান না করে ব্যবহারকারীকে জিজ্ঞাসা করতে হবে।

---

## ধাপ ১ — প্রথমে রুট-কজ ডায়াগনসিস (কোনো ফিক্স না, শুধু তদন্ত) — এটা প্রথম সেশনের প্রম্পট

```
প্রম্পট (সেশন ১ — শুধু ডায়াগনসিস, কোনো কোড এডিট না):

SOMADHAN_LOADING_PATTERN_MASTER_PROMPT.md পড়ে ৪টা কোর কম্পোনেন্ট বুঝে নাও। এখন নিচের দুটো বাগের
রুট-কজ খুঁজে বের করো, কোনো কোড এডিট না করে শুধু রিপোর্ট লেখো:

১. "স্টাক শিমার" বাগ: ইউজার ওয়ালেটে রিচার্জ সম্পন্ন করার পর পুরো ওয়ালেট পেজ শুধু শিমার-ই লোড হয়ে
   যাচ্ছে, কখনো শেষ হচ্ছে না (অন্য পেজে গিয়ে আবার এই পেজে এলে ঠিক হয়ে যায়)। খুঁজে বের করো:
   - কোন syncPhase/state variable রিচার্জের পরে LOADING-এ সেট হচ্ছে কিন্তু কোনো কোড-পথে আর
     LOADED-এ ফিরছে না (যেমন: কোনো callback মিস হচ্ছে, exception silently swallow হচ্ছে,
     কোনো coroutine cancel হয়ে যাচ্ছে navigation-এ, অথবা realtime event না আসায় phase আটকে আছে)।
   - এটা কি UserWalletScreen.kt/SomadhanRepository.kt-এর সব জায়গায় একই প্যাটার্নে ঘটছে, নাকি শুধু
     রিচার্জ-ফ্লো-স্পেসিফিক কোনো কোড-পথে?
   - grep দিয়ে খুঁজে বের করো এই একই প্যাটার্ন (action → phase সেট হয় → নির্দিষ্ট শর্তে কখনো রিসেট
     হয় না) আর কোন কোন স্ক্রিনে/ফ্লোতে থাকতে পারে (withdrawal request, post problem, bid submit,
     ইত্যাদি action-ভিত্তিক ফ্লোগুলোতেও একই রিস্ক আছে কিনা)।

২. "random auto full-page shimmer" বাগ: কোনো user action ছাড়াই মাঝে মাঝে বিভিন্ন পেজে পুরো পেজ
   আবার শিমার হয়ে যাচ্ছে। খুঁজে বের করো:
   - `initialSyncPhase`/অন্য কোনো shared sync-phase কি মাঝে মাঝে LOADED থেকে আবার LOADING-এ ফিরে
     যাচ্ছে (উদাহরণ: reconnect/network-blip/token-refresh/realtime-resubscribe ট্রিগারে)?
   - `SyncAwareContent`-এর কোনো `sessionKey`/`viewModel.hasLoadedOnce(...)` লজিকে কোনো রেস
     কন্ডিশন আছে কিনা যার ফলে "already loaded" state ভুলবশত রিসেট হয়ে যাচ্ছে।
   - এটা কি নির্দিষ্ট trigger-এ (app background→foreground, connectivity change, token refresh)
     reproducible, নাকি সত্যিকারের random/race-condition?

আউটপুট: কোনো zip না, শুধু একটা টেক্সট রিপোর্ট (চ্যাটে বা একটা .md ফাইলে) — প্রতিটা বাগের রুট-কজ,
কোন ফাইলের কোন ফাংশনে সমস্যা, আর প্রস্তাবিত ফিক্সের সংক্ষিপ্ত approach (কোড লেখার দরকার নেই)।
কোনো ফাংশনালিটি বদলানো যাবে না যেহেতু এই ধাপে কোনো এডিটই হচ্ছে না।
```

এই রিপোর্ট পাওয়ার পর ব্যবহারকারী কনফার্ম করবে, তারপর ধাপ ২ থেকে আসল ফিক্স শুরু হবে। **যদি এই ধাপ
সময়ের অভাবে/কনটেক্সট শেষ হয়ে অসম্পূর্ণ থাকে, progress note-এ লিখতে হবে ঠিক কোন বাগের ডায়াগনসিস শেষ
হলো, কোনটা বাকি — যাতে পরের সেশন বাকিটা থেকে শুরু করতে পারে, পুরোটা আবার না করে।**

---

## ধাপ ২ — স্ক্রিন-ভিত্তিক বাগ তালিকা (প্রতিটা একটা আলাদা সেশন-এর প্রম্পট হবে)

> নিচের ফাইলনামগুলো zip স্ক্যান করে অনুমান করা — আসল এডিট শুরুর আগে প্রতিটা সেশনকে Ground Rule ১৩
> অনুযায়ী `grep`/`view` দিয়ে নিজে যাচাই করে নিতে হবে (স্ক্রিন রিফ্যাক্টর হয়ে থাকতে পারে)।

### সেশন প্রম্পট ২.১ — Dashboard (`DashboardScreen.kt`)
```
DashboardScreen.kt-এ rule ২ (re-entry) ঠিকমতো কাজ করছে না। প্রথম visit-এ ফুল-পেজ স্কেলিটন ঠিক আছে,
কিন্তু re-entry-তে পুরো পেজ আবার লোড না হয়ে শুধু নিচের value/list অংশগুলো শিমার হওয়া উচিত:
- মোট পোস্ট সংখ্যার ভ্যালু
- মোট সম্পন্ন কাজের ভ্যালু
- নিচের ক্যাটাগরি-অনুযায়ী পোস্ট-বিভাজনের লিস্ট
Pull-to-refresh-এ একই তিনটা অংশ (শুধু এগুলোই) শিমার হবে, বাকি কাঠামো (header, layout) অপরিবর্তিত
থাকবে। **নোট:** `DashboardScreen.kt` একটা শেয়ার্ড স্ক্রিন — `isSolver` ফ্ল্যাগ দিয়ে user/solver
দুটো role-ই একই ফাইল ব্যবহার করে (সলভারের বেলায় হেডার/ভ্যালুগুলো ভিন্ন হতে পারে)। তাই এই ফিক্স
করার সময় দুই role-branch-ই grep করে verify করতে হবে যে শিমার/re-entry আচরণ সোলভার-সাইডেও ঠিকমতো
কাজ করছে (ধাপ ০.৫ দেখো)। rule ১ (cold load) এখন ঠিক আছে বলে ব্যবহারকারী কনফার্ম করেছেন — সেটা না ছুঁয়ে শুধু rule
২+৩ ফিক্স করো। rememberFieldChangePulse/SyncAwareContent-এর existing প্যাটার্ন অনুসরণ করো
(অন্য স্ক্রিনে যেমন ব্যবহার হয়েছে সেভাবেই, নতুন মেকানিজম বানানোর দরকার নেই)। শিমারের shape এই তিনটা
section-এর real layout-এর সাথে মিলতে হবে (ধাপ ০.১৫ দেখো)। কাজ শেষে: brace-balance verify, grep
দিয়ে নিশ্চিত করো পুরনো broken pattern আর নেই, পুরো প্রজেক্টের zip (ফাইল-সংখ্যা/dotfile মিলিয়ে),
আর progress note আপডেট।
```

### সেশন প্রম্পট ২.২ — Emergency/জরুরি মেনু (`InstantJobsScreen.kt`, `InstantJobHistoryScreen.kt`)
```
জরুরি (Instant Jobs) মেনুতে প্রথম visit-এ ফুল-পেজ শিমার ঠিক আছে। re-entry-তে সমস্যা: পুরো পেজ আবার
লোড হচ্ছে, কিন্তু কাঙ্ক্ষিত আচরণ হলো শুধু location-সংক্রান্ত section-টা শিমার হবে, বাকি সব কাঠামো
অপরিবর্তিত থাকবে। pull-to-refresh-এও একই (শুধু location section)।
এছাড়া InstantJobHistoryScreen.kt (history ট্যাব)-এ এখন pull-to-refresh আছে যেটা থাকা উচিত না —
এই ট্যাবে pull-to-refresh সম্পূর্ণ বন্ধ করো (বাকি সব আচরণ যেমন আছে থাকবে, শুধু style এর সাথে
listগুলো না-মেলার সমস্যাটাও এই সুযোগে ঠিক করো — শিমারের shape history list-item-এর real card
layout-এর সাথে মেলানো)। **নোট (grep-verified):** `InstantJobsScreen.kt` (জরুরি জব হাব) আসলে
সোলভার-অনলি স্ক্রিন (হার্ডকোডেড `isSolver = true`) — এখানে আলাদা কোনো user-সাইড সমতুল্য পেজ নেই,
তাই এই স্ক্রিনের জন্য অতিরিক্ত কিছু করার দরকার নেই। কিন্তু `InstantJobHistoryScreen.kt` একটা
শেয়ার্ড স্ক্রিন (`isSolver` চেক দিয়ে user/solver দুই role-ই ব্যবহার করে) — তাই এই ফাইলের ফিক্স করার
সময় দুই role-branch-ই grep করে verify করতে হবে (ধাপ ০.৫ দেখো)।
```

### সেশন প্রম্পট ২.৩ — Wallet মূল পেজ (`UserWalletScreen.kt`) + স্টাক-শিমার ফিক্স
```
ধাপ ১-এর ডায়াগনসিস রিপোর্ট অনুযায়ী রিচার্জ-পরবর্তী স্টাক-শিমার বাগটা এখানে ফিক্স করো (root-cause
রিপোর্টে যা পাওয়া গেছে সেই অনুযায়ী — phase reset মিসিং জায়গায় ঠিকমতো LOADED-এ ফেরানো, exception
swallow হলে সেটা handle করা, ইত্যাদি)।
এছাড়া:
- re-entry-তে শুধু balance amount আর transaction list শিমার হবে, বাকি পেজ কাঠামো (header, action
  বাটনগুলো) অপরিবর্তিত থাকবে (এখন majhe majhe পুরো পেজ auto-reload হয়ে যাচ্ছে — এটাই ধাপ ০.১৪(খ)-এর
  systemic বাগ, এখানেও চেক করো)।
- pull-to-refresh-এ শুধু balance + transaction list শিমার হবে।
- transaction history-এর ট্যাব পরিবর্তন করলে প্রতিবার শুধু transaction list অংশটাই শিমার হবে, বাকি
  পেজ অপরিবর্তিত থাকবে (এখন কি হচ্ছে তা grep করে verify করে তারপর ফিক্স করো)।
- রিচার্জ সম্পন্ন হওয়ার পরে infinite-shimmer বাগ (উপরে বলা হলো) — এটাই এই সেশনের সবচেয়ে গুরুত্বপূর্ণ
  ফিক্স, খুব সাবধানে টেস্ট-কেস (grep দিয়ে কোড-পাথ ট্রেস করে) verify করো।

**নোট (grep-verified):** `UserWalletScreen.kt` একটা শেয়ার্ড স্ক্রিন — `isSolver` ফ্ল্যাগ দিয়ে
user/solver দুই role-ই এই একই ফাইল ব্যবহার করে (আলাদা কোনো `SolverWalletScreen.kt` নেই)। তাই এই
সেশনের সব ফিক্স (স্টাক-শিমার, re-entry, pull-to-refresh, ট্যাব-চেঞ্জ) দুই role-branch-ই grep করে
verify করতে হবে — শুধু user-role টেস্ট করে যথেষ্ট মনে করা যাবে না (ধাপ ০.৫ দেখো)।
```

### সেশন প্রম্পট ২.৪ — Wallet "আরো দেখুন"/Transaction History (`TransactionHistoryScreen.kt`)
```
প্রথম visit-এ ফুল-পেজ ঠিক আছে। re-entry-তে শুধু transaction history list শিমার হওয়া উচিত (পুরো পেজ
না)। ট্যাব change করলেও শুধু transaction list অংশ শিমার হবে। pull-to-refresh-এও শুধু transaction
list। বাকি কাঠামো (header, tab bar নিজে) সবসময় অপরিবর্তিত/visible থাকবে rule ৩/৪ অনুযায়ী।

**নোট (grep-verified):** `TransactionHistoryScreen.kt` একটা শেয়ার্ড স্ক্রিন (`isSolver` চেক দিয়ে
user/solver দুই role-ই ব্যবহার করে, filter-logic-ও role অনুযায়ী ভিন্ন)। তাই ফিক্স করার সময় দুই
role-branch-ই grep করে verify করতে হবে (ধাপ ০.৫ দেখো)।
```

### সেশন প্রম্পট ২.৫ — Withdrawal (`UserWithdrawScreen.kt` / `SolverBalanceWithdrawScreen.kt`, `WithdrawalHistoryScreen.kt`)
```
এই পেজের বাকি সব আচরণ ঠিক আছে (ব্যবহারকারী কনফার্ম করেছেন) — শুধু pull-to-refresh মেকানিজম যোগ করতে
হবে, যেটাতে re-entry-এর মতোই শুধু transaction/withdrawal history list শিমার হবে, বাকি কাঠামো
অপরিবর্তিত। অন্য কোনো existing আচরণ বদলানো যাবে না (Ground Rule ১)।
```

### সেশন প্রম্পট ২.৬ — Profile (`ProfileScreen.kt`)
```
প্রথম visit-এ ফুল-পেজ লোড ঠিক আছে, re-entry-তে কোনো লোডিং/শিমার হবে না (এটাও এখন ঠিক আছে বলা
হয়েছে)। শুধু একটা জিনিস ঠিক করো: এই পেজে pull-to-refresh সম্পূর্ণ বন্ধ/সরিয়ে ফেলো (ব্যবহারকারীর
স্পষ্ট নির্দেশ — profile পেজে pull-to-refresh থাকবে না)। এতে অন্য কোনো আচরণ/ফাংশনালিটি না বদলায়
সেটা নিশ্চিত করো (শুধু pull-gesture handler সরানো, ডাটা-লোড লজিক অপরিবর্তিত)।

**নোট (grep-verified):** `ProfileScreen.kt` একটা শেয়ার্ড স্ক্রিন — user/solver দুই role-ই এই একই
ফাইল ব্যবহার করে (role-নির্দিষ্ট ডেটা যেমন `solverBids`/`myFavoriteSolversCount` ভেতরেই branch
হয়)। তাই pull-to-refresh সরানোর ফিক্স দুই role-branch-ই grep করে verify করতে হবে যাতে কোনো
role-এর জন্য অপ্রত্যাশিতভাবে অন্য কোনো আচরণ না বদলায় (ধাপ ০.৫ দেখো)।
```

### সেশন প্রম্পট ২.৭ — Account > সকল সমস্যা মেনু + Problem Detail + বিড লিস্ট (`UserProblemsScreen.kt`, `SolverProblemsScreen.kt`, `ProblemDetailScreen.kt`, `SolverAllPostsScreen.kt`/`SolverMyBidsScreen.kt`, `BidManagementScreen.kt`)
```
"সকল সমস্যা" (account) মেনুতে re-entry-তে শুধু পোস্ট-লিস্ট শিমার হওয়াটা কাঠামোগতভাবে ঠিক আছে —
কিন্তু শিমারের style আসল পোস্ট-কার্ড layout-এর সাথে মিলছে না, এটা মেলাও (ধাপ ০.১৫)। এই একই
বাগ-ফিক্স (re-entry-তে শুধু পোস্ট-লিস্ট শিমার + শিমার-shape মেলানো) সোলভার রোলের সমতুল্য পেজ
`SolverProblemsScreen.kt` (সোলভারের "সকল সমস্যা/বিড লিস্ট" পেজ)-এও প্রযোজ্য — এখানেও একই কাঠামোয়
ফিক্স করো।

এই লিস্টের কোনো একটা পোস্টে ঢুকে ProblemDetailScreen.kt-এ যাওয়ার ফ্লোতে **আসল বাগ**: প্রথম visit-এ
ফুল-পেজ লোড হয়ে ডিটেইলস দেখানো ঠিক আছে, কিন্তু re-entry করলে (এই ডিটেইল পেজে দ্বিতীয়বার ঢুকলে) পুরো
পেজ আবার লোড হওয়া শুরু করে এবং **কখনো থামে না, ডিটেইলস দেখায় না** — এটা critical bug, আগে fix করা
দরকার। খুঁজে বের করো কেন re-entry-তে loading phase কখনো LOADED-এ পৌঁছাচ্ছে না (এই একই ক্লাসের বাগ
ধাপ ১-এর ওয়ালেট-রিচার্জ ডায়াগনসিসের সাথে সম্পর্কিত হতে পারে — একই root cause pattern কিনা যাচাই
করো)। ফিক্স করার পর কাঙ্ক্ষিত আচরণ: re-entry-তে পুরো পেজ কাঠামো অপরিবর্তিত রেখে শুধু bid list অংশ
শিমার হবে, বাকি সব (problem details) সবসময় visible/অপরিবর্তিত থাকবে।

একই bug pattern + একই fix "আমার পোস্ট" (SolverAllPostsScreen.kt/SolverMyBidsScreen.kt) মেনুতে এবং
BidManagementScreen.kt-এও আছে — একই approach দিয়ে দুটোতেই ফিক্স করো।
```

### সেশন প্রম্পট ২.৮ — পছন্দের সমাধানকারী (`FavoriteSolversScreen.kt`)
```
প্রথম visit-এ শুধু ফুল-পেজ শিমার হচ্ছে যেটা ঠিক আছে, কিন্তু re-entry-তে শুধু তালিকা অংশটাই শিমার
হওয়া উচিত (এখন পুরো পেজ লোড হচ্ছে কিনা যাচাই করো)। pull-to-refresh সম্পূর্ণ নতুন যোগ করতে হবে —
refresh করলে শুধু পছন্দের সমাধানকারীর তালিকা অংশ শিমার হবে, বাকি কাঠামো অপরিবর্তিত।

**নোট (ব্যবহারকারী কনফার্ম করেছেন):** `FavoriteSolversScreen.kt` সম্পূর্ণভাবে user-সাইড-only
ফিচার — সোলভার রোলে এর কোনো সমতুল্য পেজ (favorite/pin লিস্ট) নেই এবং বানানোরও দরকার নেই। এই সেশনে
শুধু এই একটা ফাইলেই ফিক্স করলেই চলবে।
```

### সেশন প্রম্পট ২.৯ — FAQ (`FaqScreen.kt`)
```
FAQ পেজের সব ফাংশনালিটি ঠিক আছে — শুধু শিমারের style পেজের real FAQ list layout-এর (accordion/
card যেমনই হোক) সাথে মিলছে না। শুধু visual/skeleton-shape ফিক্স, কোনো লজিক বদলাবে না।

**নোট (grep-verified):** `FaqScreen.kt` একটা শেয়ার্ড স্ক্রিন — user/solver দুই role-ই এই একই ফাইল
ব্যবহার করে (`userRole` অনুযায়ী শুরুর ট্যাব/কনটেন্ট ভিন্ন)। তাই শিমার-shape ফিক্স করার সময় দুই
role-branch-এর FAQ layout-ই grep করে দেখে নিতে হবে যাতে shape-mismatch দুই role-এই ঠিক হয় (ধাপ
০.৫ দেখো)।
```

### সেশন প্রম্পট ২.১০ — সোলভার অ্যাকাউন্টের সাধারণ শিমার বাগ (broad audit)
```
সোলভার অ্যাকাউন্টের ভেতরে একই ক্লাসের বাগ আছে: একবার পেজ visit করে re-entry করলে মাঝে মাঝে সেই পেজ
শুধু শিমার-ই লোড হতে থাকে, শেষ হয় না (কোনো নির্দিষ্ট একটা পেজ না — মাঝে মাঝে বিভিন্ন পেজে হয়)।
এটা ধাপ ০.১৪(ক)-এর systemic "স্টাক শিমার" প্যাটার্নেরই সোলভার-সাইড instance হতে পারে। SolverKycScreen,
SolverSkillsScreen, SolverCategoryPostsScreen, SolverCompletedJobsScreen, SolverReviewsScreen —
প্রতিটাতে grep দিয়ে verify করো কোথায় একই "phase কখনো LOADED-এ ফেরে না" প্যাটার্ন আছে, এবং ধাপ
১-এর ডায়াগনসিসে পাওয়া fix pattern প্রয়োগ করো। একসাথে সব স্ক্রিন না ছুঁয়ে একটা একটা করে, প্রতিটার পর
brace-balance + grep verify + progress note।
```

### সেশন প্রম্পট ২.১১ — KYC সেলফি ক্যামেরা পারমিশন বাগ (আলাদা, শিমার-সম্পর্কিত না)
```
SolverKycScreen.kt-এ সেলফি তোলার জন্য ক্যামেরা বাটনে ক্লিক করলে, যদি ক্যামেরা পারমিশন আগে থেকে না
থাকে, তাহলে অ্যাপ পারমিশন request popup দেখাচ্ছে না — সরাসরি error দেখাচ্ছে। ঠিক করো যাতে পারমিশন
না থাকলে standard Android permission-request flow (rememberLauncherForActivityResult /
ActivityResultContracts.RequestPermission) ট্রিগার হয়, ইউজার allow করলে সাথে সাথে ক্যামেরা খুলবে,
deny করলে সুনির্দিষ্ট (rationale সহ) মেসেজ দেখাবে। এটা shimmer-system-এর অংশ না, স্বাধীনভাবে ছোট
এই একটা ফিক্স।
```

### ইচ্ছাকৃতভাবে **বাদ** (এখন করা হবে না)
```
Admin panel-এর শিমার বাগ — ব্যবহারকারী স্পষ্ট বলেছেন এটা এখন ঠিক করার দরকার নেই, তিনি পরে নিজে
দেখবেন। কোনো সেশনে AdminPanelScreen.kt/Admin*View.kt ফাইলগুলোতে হাত দেওয়া যাবে না যতক্ষণ না
ব্যবহারকারী স্পষ্টভাবে বলছেন।
```

---

## ধাপ ৩ — প্রতিটা সেশনের জন্য বাধ্যতামূলক চেকলিস্ট (আগের Ground Rules + এই ফাইলের ১৪/১৫/১৬ নম্বর সহ)

প্রতিটা সেশন প্রম্পটের শেষে এই চেকলিস্ট জুড়ে দিতে হবে (কপি করে ব্যবহার করো):

```
কাজ শেষে বাধ্যতামূলক:
1. এডিট করা প্রতিটা ফাইলে { } ( ) [ ] ব্যালেন্স ম্যানুয়ালি গুনে যাচাই করো (Kotlin-aware:
   string/string-template/comment স্কিপ করে)।
2. grep দিয়ে নিশ্চিত করো পুরনো broken/dead প্যাটার্ন (যদি সরানো হয়) আর কোথাও নেই, আর নতুন
   ইম্পোর্ট/ফাংশন যা ব্যবহার করেছো তা আগে থেকেই MotionToolkit.kt/CommonComponents.kt-এ বিদ্যমান
   কিনা যাচাই করো (নতুন হেল্পার লাগলে সেটাও উল্লেখ করে জানাও)।
3. এই সেশনে নাম-করা বাগ ছাড়াও, স্পর্শ করা প্রতিটা ফাইলে ধাপ ০.১৪(ক)/(খ)-এর দুটো systemic প্যাটার্ন
   আছে কিনা quick-check করো — পেলে ছোট আলাদা commit হিসেবে ফিক্স করো, প্রোগ্রেস নোটে আলাদা করে লেখো।
4. কোনো বিদ্যমান ফাংশনালিটি/বিজনেস-লজিক/নেভিগেশন/পারমিশন আচরণ বদলাবে না (Ground Rule ১)।
4a. এই স্ক্রিন শেয়ার্ড (user+solver একই ফাইল) হলে ধাপ ০.৫ অনুযায়ী দুই role-branch-ই আলাদাভাবে grep
    করে verify করো; আলাদা সোলভার-ফাইল থাকলে সেখানেও একই ফিক্স প্রয়োগ হয়েছে কিনা নিশ্চিত করো। কোনো
    স্ক্রিনের role-equivalent নিয়ে অস্পষ্টতা থাকলে অনুমান না করে ব্যবহারকারীকে জিজ্ঞাসা করো, উত্তর
    পাওয়ার পরই এগোও (Ground Rule ১৬ — এই নিয়ম লঙ্ঘনযোগ্য না)।
5. পুরো প্রজেক্টের zip বানাও (শুধু বদলানো ফাইল না) — dotfile সহ কোনো ফাইল miss না করে, আগের
   zip-এর ফাইল-লিস্টের সাথে diff করে ফাইল-সংখ্যা/নাম মিলিয়ে যাচাই করে।
6. zip-এর ভেতরে progress note আপডেট/যোগ করো: এই সেশনে কী করা হলো, কী verify করা হলো, পরের সেশনের
   জন্য কী বাকি। যদি এই সেশন সময়/কনটেক্সটের অভাবে কাজ অর্ধেক রেখে zip দিতে হয়, তাহলে ঠিক কোন অংশ
   শেষ হয়েছে আর কোনটা বাকি তা স্পষ্ট করে লেখো, যাতে পরের সেশন প্রথমে বাকি অংশ শেষ করে, তারপর
   ব্যবহারকারীর কনফার্মেশন নিয়ে পরের ধাপে যায়।
7. real Android Studio/Gradle build এই sandbox-এ সম্ভব না — এটা ব্যবহারকারীকে প্রতিবার স্পষ্টভাবে
   মনে করিয়ে দাও, "সব ঠিক আছে" কখনো claim করবে না যতক্ষণ না real build verify হয়।
```

---

## ধাপ ৪ — সাজেস্টেড অর্ডার

১ (ডায়াগনসিস, বাধ্যতামূলক প্রথমে) → ২.১১ (KYC, স্বাধীন/সহজ, দ্রুত শেষ করা যায়) → ২.৩ (Wallet,
সবচেয়ে জটিল/গুরুত্বপূর্ণ, স্টাক-শিমার রুট-কজ ফিক্স) → ২.৭ (Problem Detail re-entry বাগ, একই
root-cause class) → ২.১, ২.২, ২.৪, ২.৫, ২.৬, ২.৮, ২.৯ (বাকিগুলো, প্রায়োরিটি স্বাধীন) → ২.১০
(broad audit, সবার শেষে যাতে আগের ফিক্সগুলোর pattern reuse করা যায়)।

প্রতিটা ধাপ শেষে ব্যবহারকারীর কনফার্মেশন নিয়ে পরেরটায় যাওয়া হবে — একসাথে সব করা হবে না।
